# 06. 개발 환경 컨텍스트

> 개발 관련 작업(코드 작성, 스크립트, 명령 안내, 트러블슈팅) 시 이 환경을 전제로 한다. 코드·명령을 제시할 때는 아래 OS·도구·규칙에 맞춰 작성한다.

## 1. 개발자 환경 (전제)
| 항목 | 값 |
|---|---|
| OS | **Windows 11 Pro (build 26100)**, 기본 셸 **CMD** (PowerShell 보조) |
| 개발자 | 1인 풀스택 (기획 겸임). Python·Node.js·Firebase·Next.js+Supabase 경험 보유 |
| 에디터 | VS Code + Claude Code 병행 (리포 루트의 CLAUDE.md가 Claude Code 지침) |
| 프로젝트 경로 | **`C:\Users\Ksd\Desktop\wstory`** — 폴더명은 `wstory`, package name 은 `weddingclear` |
| 형상관리 | Git + GitHub(nomadconan/wedding). main(배포)/dev(통합)/기능 브랜치, PR 병합 |
| 배포 | Vercel(웹, icn1 리전) + Supabase 클라우드(스테이징/프로덕션 프로젝트 분리) |

## 2. 런타임·도구 버전

아래는 **실측값**(2026.8 확인)이다. 갱신할 때는 추측하지 말고 다음으로 확인한다:
`node -v` / `npm -v` / `git --version` / `docker --version` / `npx supabase --version`

| 도구 | 버전·설치 | 용도 |
|---|---|---|
| Node.js | **v20.20.2 (20 LTS)** — nvm-windows로 고정 | Next.js 실행 |
| npm | **10.8.2** (Node 동봉). 패키지 매니저는 **npm 단일** — pnpm/yarn 혼용 금지 | 의존성 |
| Next.js | 14.2.35 | 웹 프레임워크 |
| Docker | **29.6.2** (Docker Desktop, WSL2 백엔드) | Supabase 로컬 스택 |
| Supabase CLI | **2.111.0** — devDependency. **전역 PATH에 없다. `npx supabase` 로 호출** | 로컬 DB/Auth/Storage, 마이그레이션 |
| Git | **2.55.0.windows.3** | 형상관리 |
| GitHub CLI | **2.97.0** (`winget install --id GitHub.cli`). 설치 후 **새 터미널**에서 PATH 반영. 인증은 `gh auth login` 필요 | PR·이슈 작업 |
| Android Studio + JDK 17 | Capacitor 앱 착수 시(현재 미설치) | 안드로이드 빌드 |
| Python 3.x | 보조 스크립트용 | 데이터 수집·문서 처리 배치 |

### Node 버전 고정 방침 (2026.8 · T-00b)

**Node 20 LTS(v20.20.2)에 고정한다.** nvm-windows로 관리하며 리포에 세 곳으로 못박아 뒀다.

| 위치 | 값 | 역할 |
|---|---|---|
| `.nvmrc` | `20.20.2` | 버전 선언의 단일 진실. CI(`actions/setup-node` 의 `node-version-file`)와 mac/Linux nvm 이 읽는다 |
| `package.json` 의 `engines.node` | `>=20 <21` | 메이저 이탈 차단. Vercel 이 빌드 런타임 선택에 참고한다 |
| (예정) CI 워크플로 `setup-node` | `node-version-file: .nvmrc` | S8-05에서 연결한다 |

> **주의(nvm-windows)** 설치본은 **nvm-windows 1.2.2** 이고, 이 버전은 인자 없는 `nvm use` 로 **`.nvmrc` 를 읽지 못한다**(mac/Linux 용 nvm 과 다른 점). 윈도우에서는 버전을 명시해서 전환한다:
>
> ```bat
> nvm use 20.20.2
> ```
>
> `.nvmrc` 는 그래도 둔다 — CI·Vercel·다른 OS 개발자가 참조하는 **선언적 단일 진실**이기 때문이다. 값을 바꿀 때는 `.nvmrc` 와 `engines` 를 함께 고친다.

**근거**

1. **Next.js 14 지원 범위** — Next 14는 Node 18.17+ 를 요구하며 **검증된 조합은 18·20 LTS**다. 24.x 는 지원 매트릭스 밖이라 문제가 생겨도 프레임워크 쪽 보증이 없다.
2. **Vercel 배포 런타임과 로컬 일치** — Vercel의 Node 런타임 선택지가 LTS 기준이라, 로컬만 24.x 로 앞서가면 **로컬은 되는데 배포는 깨지는** 상황을 만든다. 배포 대상과 같은 메이저를 쓰는 편이 안전하다.
3. **CI 재현성** — `npm ci` 가 로컬·CI·배포에서 같은 결과를 내야 한다. 메이저가 갈리면 네이티브 의존성 재빌드에서 차이가 난다.

**버전을 올릴 때**는 세 곳(`.nvmrc` · `engines` · CI)을 함께 바꾸고, Next 지원 매트릭스와 Vercel 런타임 선택지를 먼저 확인한다.

> **주의(Windows·nvm)** nvm-windows는 `NVM_SYMLINK`(예: `C:\nvm4w\nodejs`)를 PATH에 걸어 버전을 전환한다.
> **버전 전환 후에는 터미널을 새로 열어야** PATH가 반영된다. 이미 떠 있던 셸·에디터·에이전트 프로세스는
> 이전 환경 변수를 그대로 물고 있어 `node` 를 못 찾거나 옛 버전을 가리킬 수 있다.

## 3. 명령 규칙 (Windows CMD 기준)
- 코드·안내에서 명령은 **CMD 문법**으로 제시한다: `copy`(cp 아님), `\` 경로 구분, `%VAR%` 환경변수, `::` 주석. bash 전용 문법(`export`·`$VAR`·heredoc) 사용 금지.
- Supabase CLI는 전역 PATH에 없다. 반드시 `npx supabase ...` 형태로 호출한다.
- 반복 작업은 `scripts\*.bat`으로 스크립트화한다. 배치 파일은 `@echo off` + `call npm ...` 패턴.
- Python 보조 스크립트는 **소스 ASCII-only** 원칙(주석·문자열에 한글 직접 삽입 금지) — Windows CMD 인코딩 사고 방지.
- 파일 인코딩: 소스·문서 UTF-8. 한글 파일명 허용(문서·산출물), 코드 파일명은 영문.

## 4. 프로젝트 표준 명령 (package.json scripts)
| 명령 | 동작 |
|---|---|
| `npm run dev` | Next.js 개발 서버 (localhost:3000) |
| `npm run build` / `start` | 프로덕션 빌드/실행 |
| `npm run lint` / `test` | ESLint / vitest (lib/core 단위 테스트) |
| `npm run db:start` | 로컬 Supabase 기동 (Docker 필요) |
| `npm run db:reset` | migrations + seed.sql 재적용 (로컬 DB 초기화) |
| `npm run db:diff -- -f <이름>` | 스키마 변경 → 마이그레이션 파일 생성 |
| `npm run db:types` | `types/database.ts` 타입 재생성 |
| `npm run assets:gen` / `assets:check` | 자산 자리표시 생성 / 매니페스트·파일·픽셀 일치 검사 (T-02c) |
| `npm run env:sync` / `env:check` | `supabase status` 값으로 `.env.local` 생성 / 검증 (S0-01) |
| `npm run seed:accounts` | **로컬 확인용 계정·업체 데이터 생성** (S0-02, 아래 §5-1) |
| `npm run db:rls` | **RLS 점검** — psql 세션을 전환해 DB 가 정말 막는지 확인한다 (S3-01, S3-04 장바구니·찜, S3-03 공개 카탈로그, S3-05 쓰기 경로, S3-08 참가격 지수, S3-09 삭제 요청, S4-01 채팅·문의, S4-04 실시간 publication). **`db:reset` → `seed:accounts` → `db:rls` 로 끊김 없이 돈다**(S4-04 이전에는 손으로 온보딩을 밟아야 했다 — 깨끗한 DB 에서 못 도는 RLS 검사는 가장 필요한 순간에 못 도는 검사다). 시험 데이터는 **스크립트가 트랜잭션 안에서 만들고 되돌린다** — 시드에 박으면 픽스처와 충돌하고 빈 화면을 볼 수 없게 된다 |
| `npm run verify` | **CI 의 빠른 게이트를 로컬에서 그대로 돈다** — lint · typecheck · test · check:secrets · check:coverage-table · assets:check |
| `npm run typecheck` | `tsc --noEmit` (S8-05 게이트 · FIX-19 해소로 **0건**) |
| `npm run check:secrets` / `check:bundle` | 시크릿 스캔 (소스 / 빌드 산출물 · §7.2) |
| `npm run check:audit` | 의존성 취약점 — `security/audit-allowlist.json` 에 **없는** high/critical 이면 실패 |
| `npm run check:coverage-table` | `docs/TASKS.md` 커버리지 요약과 본문 행의 불일치 검사 |
| `npm run check:tables` | **문서 표의 칸 수가 머리글과 맞는지** 센다(`verify` 포함). 이스케이프 안 된 `\|` 로 행이 잘리는 사고가 넷 있었다 |
| `npm run test:coverage` | 커버리지 80% — **`@vitest/coverage-v8` 미설치라 아직 안 돈다**(D-110) |
| `npm run audit:routes` | 명세 §6·커버리지 표 D·실제 라우트를 대조하고 **내비 링크·도달 가능성**을 전수로 센다 (정적) |
| `npm run audit:screens` | **실제 Chrome 을 CDP 로 몰아** 로그인 폼을 지나 화면을 연다 (계정 10축 × 화면 97) |
| `npm run audit:api` | 화면 단계에서 걷은 세션 쿠키로 `route.ts` 를 전수로 친다 (**쓰기를 남긴다** — 아래 복구 절차) |
| `npm run audit:runtime` | 위 둘을 한 번에 |
| `npm run audit:report` | 위 결과를 읽어 `docs/ROUTES.md` 의 표를 만든다 |
| `npm run check:escrow` | **에스크로 자동 릴리즈 배치를 실제로 두드린다**(FIX-14). `?now=` 를 옮겨 가며 기한 전·예식 전·**예식 당일**에 조용한지 먼저 보고 그 다음 릴리즈를 본다 |
| `npm run check:settlement` | **정산 기간 집계 배치를 실제로 두드린다**(FIX-08). 열린 홀드가 있으면 정산에서 빠지고 `escrow-release` 뒤에는 든다 — 두 배치의 순서 의존을 눈으로 본다 |
| `npm run check:no-store` | **서버 조회가 굳지 않는지 실제로 본다**(FIX-22). 값을 바꾸고 화면이 따라오는지, 굳어야 하는 `/guides` 는 그대로 굳는지 |
| `scripts\dev-setup.bat` | 최초 셋업 일괄 실행 |

> **`audit:*` 와 `check:escrow`·`check:settlement` 는 DB 를 더럽힌다.** 인가가 본문 검증보다
> 앞에 있는지 보려면 쓰기를 쳐야 하고, 배치는 상태를 옮겨야 확인되기 때문이다.
> **끝나면 반드시 되돌린다** — 건너뛰고 `db:rls` 를 돌리면 깨진다(실제로 겪었다):
>
> ```bat
> npm run db:reset
> npm run seed:accounts
> ```
>
> **서버가 떠 있어야 한다.** 그리고 **`npm run dev` 가 아니라 프로덕션 빌드로** 돈다 —
> Windows 에서 `next dev` 가 요청 폭주에 전 라우트를 500 으로 만든 일이 있다(FIX-58).
>
> ```bat
> npm run build
> set ENABLE_DEV_ROUTES=true && npm start
> ```

> `npm run db:*` 는 npm이 `node_modules\.bin` 을 PATH에 넣어주므로 그대로 동작한다.
> **터미널에서 직접 부를 때는 `npx supabase ...`** 를 쓴다(예: `npx supabase status`).

## 5. 로컬 Supabase 접속 정보
| 항목 | 값 |
|---|---|
| Project URL | http://127.0.0.1:54321 |
| DB URL | postgresql://postgres:postgres@127.0.0.1:54322/postgres |
| Studio | http://127.0.0.1:54323 |
| Mailpit(메일 확인) | http://127.0.0.1:54324 |
| Analytics | **`supabase/config.toml` 에서 `enabled = false`** — Windows에서 Docker 데몬을 **2375 포트로 노출**해야 동작하는데, 인증 없는 원격 제어 경로가 열리므로 **보안상 열지 않는다** |

> 로컬 접속값은 `npx supabase status` 로 확인한다. 컨테이너 이름은 프로젝트 폴더명을 따라 `supabase_db_wstory` 형태다.

## 5-1. 로컬 계정 셋업 (S0-02)

화면을 눈으로 확인하려면 로그인 계정이 필요하다. `npm run seed:accounts` 가 계정과
업체 확인용 데이터를 만든다. **계정 데이터만 만들며 인증·인가 코드는 건드리지 않는다.**

```bat
npm run db:start
npm run db:reset
npm run seed:accounts
:: 비밀번호 지정:  npm run seed:accounts -- --password=my-password
:: 승인 데모 재장전: npm run seed:accounts -- --reset
```

| 계정 | `profiles.role` | 용도 |
|---|---|---|
| `admin@local.test` | `admin` | 운영자 콘솔(`/admin/**`) |
| `ops@local.test` | `ops` | 운영 담당자. admin과 같은 접근 범위 |
| `vendor@local.test` | `vendor_owner` | 업체 어드민(`/vendor/**`). 업체 1곳의 owner |
| `staff@local.test` | `vendor_staff` | 같은 업체의 담당자. **판매가·추가금·정산·멤버 변경 불가**(S2-07) |
| `couple-a@local.test` | `consumer` | 소비자. `/onboarding` 을 진행하면 커플의 **owner** 가 된다 (S3-01) |
| `couple-b@local.test` | `consumer` | 배우자 역할. A 가 만든 초대 코드를 수락해 **partner** 로 합류한다 (S3-01) |
| `couple-linked-a@local.test` | `consumer` | **연동 커플 픽스처의 owner**. 온보딩이 이미 끝나 있고(6문항·`stage=active`) 배우자와 연동돼 있다. `db:rls` 가 이 커플을 쓴다 (S4-04) |
| `couple-linked-b@local.test` | `consumer` | 같은 커플의 **partner**. 커플 공유(D-19)를 눈으로 확인할 때 쓴다 (S4-04) |
| `planner@local.test` | `planner` | **플래너 콘솔**(`/pro`)·플래너 마켓 확인. 소비자 계정을 겸하지 않는다 — 겸하면 "플래너"와 "커플 구성원"이 같은 사람이 되어 위임 격리 검사가 **엉뚱한 이유로 통과**한다. 시드가 플래너 레코드 + **활성 위임**까지 만든다 (S6-01·S6-02) |

**계정은 9개다.** `scripts/seed-accounts.mjs` 의 목록과 이 표가 진실이며 **둘이 어긋나면
스크립트가 진실**이다. (T-00h 가 `planner@local.test` 를 표에 넣었다 — S6-01 이 만들었지만
표 아래 각주에만 있었고 **표에는 8개만 있었다.**)

기본 비밀번호는 **`local-dev-1234`** 이며 실행할 때마다 콘솔에 출력된다.
`--password=` 로 바꿔 넣을 수 있다. **로컬 전용 값이다.** 리포에 계정 정보를 커밋하지 않고,
다른 환경에서 재사용하지 않는다.

> ### ✅ 로그인이 열렸다 (FIX-24 해소 · 2026-08-27 · S8-01)
>
> **한 줄 원인 — 로그인 실패에 화면 문구가 없었다.**
>
> 인증 계층(GoTrue)이 5xx 를 돌려주면 `auth-js` 가 **약 30초 동안 조용히 재시도**한다.
> 그동안 화면은 "처리 중…" 에 멈춘 채 `[role=alert]` 이 **비어 있고**, 30초 뒤 겨우 뜨는
> 문구가 **비밀번호가 틀렸을 때와 완전히 같은 한 줄**이었다. 그래서 본 사람은 자격증명을
> 의심했고 인프라를 보지 않았다.
>
> **"서버 로그를 못 찾았다" 는 것도 같은 이유다.** 로그인 POST 는 브라우저에서 **Supabase 로
> 직접** 나간다 — Next 서버를 지나지 않으므로 Route Handler·미들웨어 로그에는 **원래 아무것도
> 남지 않는다.** 위에서 찾던 "서버 로그" 는 존재할 수 없는 것이었다.
>
> **어떻게 재현했나.** 실제 Chrome 을 CDP 로 몰아 **로그인 폼을 직접 지났다.** `db:rls`(psql
> 세션)도 흐름 스크립트(PostgREST 직접 호출)도 지나지 않던 경로다 — 그래서 로그인이 막혀도
> 전부 초록불이었다. 인증 컨테이너를 내린 상태에서 **응답 502/500/503 · 쿠키 없음 · 화면 문구
> 없음**을 확보했고, 살린 상태에서는 `next dev` 와 프로덕션 빌드 양쪽에서 정상 로그인을 확인했다.
>
> **무엇을 고쳤나.**
>
> | 고친 것 | 내용 |
> |---|---|
> | 실패 분류 | `lib/core/auth/login-error.ts` — 자격증명 / 이메일 미확인 / 과다 시도 / **인증 서버** / **타임아웃** / 키 설정 |
> | 화면 | 문구 + **다음에 할 일** + **코드**를 함께 낸다. 환경 문제면 "비밀번호 문제가 아닙니다" 라고 적는다 |
> | 제한 시간 | **12초**. 30초 침묵 구간이 사라졌다 |
> | 착지 | `lib/core/auth/landing.ts` 로 꺼내고 **경로 실재를 테스트가 파일로 확인**한다 |
>
> **인증 우회로를 만들지 않았다.** 서버가 준 문장을 그대로 노출하지 않고(§5.3) **계정 열거도
> 열지 않는다** — 없는 이메일과 틀린 비밀번호는 여전히 같은 코드다. RLS·미들웨어는 손대지 않았다.
>
> **계정 6종 착지 확인 (프로덕션 빌드 · 실제 브라우저).**
>
> | 계정 | 착지 | 비고 |
> |---|---|---|
> | `admin@local.test` · `ops@local.test` | `/admin` | **바뀌었다** — 전에는 `/admin/vendors`(대시보드가 없어서) |
> | `vendor@local.test` · `staff@local.test` | `/vendor` | 그대로 |
> | `planner@local.test` | `/pro` | **바뀌었다** — 전에는 소비자 홈(`/home`)에 떨어졌다 |
> | `couple-a@local.test` | `/home` | 그대로 |
>
> **404 는 없다.** 다만 **눈으로 본 화면은 여섯**(`/login`·`/admin`·`/admin/vendors`·`/vendor`·
> `/pro`·`/home`)이고, **나머지 화면 전수 점검은 여전히 `S0-04` 의 일**이다.
>
> **FIX-22 계열이 아니었다.** 캐시가 아니라 문구의 부재였다. 다만 S8-01 이 만든 `/admin` 과
> `GET /api/admin/metrics` 는 **둘 다 `force-dynamic`** 이다 — 세션에 따라 달라지는 자리가
> 굳으면 권한을 잃은 사람에게 캐시된 지표가 계속 나간다.

**참가격 픽스처(S8-10).** `seed:accounts` 가 **승인 업체 5곳 + 각 1개 게시 상품**을 만든다
(가격을 900만~2100만으로 벌려 둔다 — p25·p50·p75 가 **서로 다른 값**이어야 백분위가 고장났을 때
티가 난다). 하한이 5곳이라 이 픽스처로 **사분위가 실제로 서고**, 표본 하나를 제외하면
**하한 아래로 떨어지는 순간**을 화면에서 볼 수 있다 — 큐레이션 화면이 존재하는 이유가 그 전이다.
**이상 탐지 임계값은 일부러 미결로 둔다**(O-19) — 큐가 `0건` 이 아니라 **`기준 미확정`** 이라고
말해야 한다. 값을 채우면 도는 것처럼 보이는 탐지기를 만들게 된다.

**분쟁 픽스처(S8-03).** `seed:accounts` 가 **예약 분쟁 1건(open)** 과 **안전거래 이의 1건(disputed)** 을
만든다(에스크로용 결제 1건 포함). **나머지 두 출처는 일부러 0으로 둔다** — 노쇼 보증금과 취소
위약금은 상담·계약 사슬이 통째로 있어야 하고, 그것을 가짜로 채우면 큐가 데이터보다 건강해 보인다.
**그 둘의 타일이 0건으로라도 떠 있는 것**이 확인할 값이다: '분쟁이 없다' 와 '큐에 안 붙어 있다' 가
같은 얼굴이면 안 된다(FIX-15 가 그렇게 몇 달을 지났다).

**개인정보 픽스처(S8-04).** `seed:accounts` 가 **기한을 8시간 넘긴 원문 1건**(일부러 `PURGE_CRITICAL_HOURS`(6)를
넘겼다 — 콘솔이 '주의' 가 아니라 **'즉시 확인'** 으로 그리는지 확인할 수 있다) · 파기 완료 1건 ·
삭제 요청 2건(대기 / 완료+사유) · **실패한 배치 실행 1건**을 만든다. 없으면 화면이 온통 0이고,
그때의 0은 **경보 규칙이 도는지 아무도 확인할 수 없다**. 실패 실행의 `error_summary` 는 `storage_error:1`
처럼 **사유:개수만** 담는다 — 경로를 적으면 그 로그가 곧 남아 있는 원문의 위치 목록이 된다.

**감사 픽스처(S8-02).** `seed:accounts` 가 `audit_logs` 4행을 만든다(승인 · 함정 · 모더레이션 ·
근거 이벤트를 단 조율 결정). **한 행이 일부러 나쁜 데이터를 담는다** — `phone` 칸과 `=HYPERLINK(...)`
로 시작하는 값이다. 콘솔은 앞을 가려야 하고 CSV 는 뒤를 무력화해야 한다. **잘 생긴 데이터만 든
픽스처는 나쁜 데이터를 다루는 코드에 대해 아무것도 증명하지 않는다.**

**지표 픽스처(S8-01).** `seed:accounts` 가 `/admin` 대시보드용으로 **퍼널 한 벌**을 더 만든다 —
공개 상품 1 · 장바구니 1(항목 1) · 문의 1 · **확정 예약 1**(총액 1,200만) · 문서 1 + 분석 1(`done`) ·
멤버십 2(활성 1 · 해지 1) · 이벤트 2. **없으면 대시보드가 온통 0** 이고, 그때의 0은 "아무 일도
없었다" 인지 "집계가 고장났다" 인지 구분되지 않는다. `db:rls` 도 같은 이유로 이 픽스처를 본다 —
값이 전부 0이면 격리 검사가 "안 보인다" 가 아니라 "없다" 라서 통과한다.
**정산은 일부러 넣지 않았다** — 수수료 금액은 `settlement.fee_basis`(O-15)에 달려 있고,
시드가 값을 채우면 **미결정이 조용히 확정**된다. 대시보드는 그 자리에서 `기준 미확정` 이라고
말해야 한다.

**정상 경로 픽스처(FIX-56 · 2026-09-03).** S0-04 가 화면 다섯을 **없는 id 로만** 열었다는
사실이 드러나 열 표를 채웠다 — `chat_rooms`(방 1 + 메시지 3: 시스템·업체·커플) ·
`community_posts`(글 2 + 댓글 2 + 업체 태그 1) · `share_links`(**살아 있는 것 1 · 거둔 것 1**) ·
`guests`(3 · 초대 토큰 1) · `vendor_invites`(대기 1) · `tasks`(4 + 간선 1) ·
`product_options`(2) · `price_rules`(2) · `consultations`(2) · `contract_cancellations`(1).
**"못 찾음 경로가 깨끗이 끝난다" 와 "정상 경로가 뜬다" 는 다른 확인**이고, 그때까지 확인된
것은 앞쪽뿐이었다.

> **픽스처는 제품이 만들 수 있는 상태만 만든다.** 커뮤니티 태그는 **승인된 업체**에만
> 붙는다 — 시드의 데모 업체는 `pending` 이라 트리거가 거절한다. 그래서 태그는 표본 업체에
> 붙이고, **'업체 답변' 배지는 승인 뒤에야 보인다**는 사실을 그대로 둔다. 검사를 통과시키려고
> 업체를 `active` 로 바꾸지 않았다 — 그것은 **제품이 만들 수 없는 상태로 통과시키는 일**이다.

> **시드가 채워지면 검사의 뜻이 바뀐다.** FIX-56 직후 `db:rls` 검사 **열다섯이 한꺼번에
> 실패**했는데 정책이 뚫린 것이 아니라 **표가 비어 있어서 통과하고 있었다**(전체 `count(*)`
> 가 1 이면 "세상에 하나뿐" 이라는 뜻이 된다). 이제 각 검사가 **자기 트랜잭션이 만든 상태로
> 센다**(D-178). 새 픽스처를 더할 때 이 방향으로 깨지면 **검사 쪽을 고친다.**

함께 만들어지는 업체 데이터 — `vendors`(**pending**) 1건, `vendor_members`(owner) 1건,
`vendor_applications`(**submitted**) 1건. 승인 전 상태로 두는 이유는 `/admin/vendors` 에서
**실제 승인 플로우를 처음부터 밟아 볼 수 있게** 하기 위해서다. 승인한 뒤 다시 해 보려면
`--reset` 을 쓴다.

> **안전장치** `NEXT_PUBLIC_SUPABASE_URL` 이 `127.0.0.1`·`localhost` 가 아니면 스크립트가
> 즉시 중단한다. 스테이징·프로덕션에 데모 계정을 만들 수 없다.

### 접근 권한이 결정되는 위치

권한 판정이 어디서 일어나는지 알아야 화면이 막힐 때 어디를 볼지 안다.

| 대상 | 1차 | 2차(역할) | 최종 경계 |
|---|---|---|---|
| **`profiles.role` 은 본인이 못 쓴다** | 운영자 승격은 **서비스롤 경유**다(S5-14 · FIX-47 · D-162) | 예전엔 `update profiles set role='admin' where user_id=auth.uid()` **한 줄로 운영자가 될 수 있었다** | **컬럼 GRANT**다 — 마이그레이션 `0068` 이 `profiles` 의 표 단위 INSERT·UPDATE·DELETE 를 걷고 `display_name`·`avatar_url`·`marketing_opt_in`·`phone_hash` 넷만 되돌렸다. `role` 은 그중에 없다. 로컬에서 운영자가 필요하면 `seed:accounts` 가 만든 `admin@local.test`·`ops@local.test` 를 쓴다 |
| `/admin/**` | `middleware.ts` — **로그인 여부만** 본다 | 화면: `requireOperator()` → `profiles.role ∈ {ops, admin}` / API: `isOperator()` → 아니면 **403** | **서버 코드**다. 운영자에게는 RLS 정책을 주지 않고 서비스롤 경유로만 접근한다(07 §3.9) |
| `/vendor/**` | `middleware.ts` — 로그인 여부만 | 화면: `requireUser()` + `vendor_members` 소속 조회. 수정 가능 여부는 `vendor_role='owner'` | **RLS**다. `is_vendor_member` / `is_vendor_owner` 가 판정하며 화면 체크는 UX 보조다 |

- **`profiles.role` 은 `/vendor/**` 접근에 쓰이지 않는다.** 업체 화면의 조건은 `vendor_members` 소속이다.
  시드가 `vendor_owner` 역할을 넣는 것은 표기용이며 인가에 관여하지 않는다.
- 미인증은 `/login?next=...` 로, 권한 부족은 `/login?next=...&denied=1` 로 보낸다.


### CI (S8-05 · 2026-08-27)

**워크플로 둘.** `.github/workflows/ci.yml`(PR·push · `dev`/`main`)와 `security.yml`(주간 감시).
파일을 하나로 두지 않은 이유는 **트리거가 다르기 때문**이다 — `npm audit` 결과는 커밋이
없어도 바뀌므로 코드가 조용한 주에도 봐야 한다. **명령은 같은 것을 쓴다**(`npm run check:audit`).

**CI 전용 명령을 만들지 않았다.** 모든 step 이 `npm run <script>` 이고 개발자가 그대로 친다.
`npm run verify` 가 빠른 게이트를 로컬에서 같은 순서로 돈다 — CI 는 통과하는데 로컬은
실패하는(혹은 반대인) 두 번째 진실을 만들지 않기 위해서다.

| 잡 | 하는 일 | 시간 | 차단 |
|---|---|---|---|
| `quality` | lint · **typecheck** · test(2016) · check:secrets · check:coverage-table · assets:check | **~1분** | 차단 |
| `quality` (마지막 step) | 커버리지 80% | — | **경고** — provider 미설치(D-110) |
| `dependency audit` | check:audit | **~40초** | 차단 |
| `integration` | `supabase start` → env:sync → env:check → **build** → **check:bundle** → seed:accounts → **db:rls**(853) | **~4분 30초** | 차단 |

**전체 실행 시간 약 4분 30초** — 셋이 병렬이라 가장 느린 `integration` 이 곧 전체다.

**`db:rls` 를 매 PR 마다 돌린다.** 맨 Postgres 서비스 컨테이너로는 안 된다 —
`auth.uid()`·`authenticated` 롤·PostgREST·GoTrue 관리 API 를 전부 쓰기 때문이다.
그래서 **로컬과 똑같이** `npx supabase start` 로 전체 스택을 띄운다(리포의 devDependency
CLI 를 쓴다 — 별도 액션을 붙이면 로컬과 CI 가 다른 CLI 버전을 쓸 수 있다).

**빌드도 그 스택을 쓴다.** `/guides` 가 `revalidate = 300` 으로 **빌드 시점에 조회**하므로
DB 없이는 빌드가 깨진다(D-113). 화면을 고쳐 우회하지 않았다 — 조회 실패를 빈 목록으로
넘기면 DB 가 잠깐 흔들린 날 **빈 가이드 페이지가 배포된다.** 덕분에 §7.2 번들 검사가
**진짜 서비스롤 키가 있는 상태**에서 돈다(키 없는 빌드를 스캔한 통과는 아무것도 증명하지 않는다).

**비밀을 워크플로에 적지 않았다.** 지금은 GitHub Secrets 를 하나도 쓰지 않는다 —
필요한 값은 방금 띄운 로컬 스택이 출력하고 `npm run env:sync` 가 받아 적는다.
스테이징·프로덕션 값이 필요해지는 날(S8-13 배포)에 Secrets 를 쓴다.

**`main` 보호 규칙에 CI 통과를 걸었다.** 기존 셋(PR 필수 · force push 금지 · 삭제 금지)에
`required_status_checks` 세 개를 더했다. `strict` 는 **끄** 두었다 — 켜면 `dev` 가 움직일
때마다 열린 PR 전부가 리베이스를 요구받고 그 리베이스가 다시 CI 를 돌려 사실상 직렬화된다.

> ⚠ **`dev` 는 여전히 보호 규칙이 없다.** 실제 병합은 전부 `dev` 로 일어나므로
> **게이트가 가장 필요한 곳은 여기**인데, 지금은 CI 가 빨개도 병합할 수 있다.
> S8-05 가 걸지 않은 이유는 사용자의 상시 작업 브랜치에 새 제약(매 병합 ~4분 30초 대기,
> 우회 불가)을 **요청 없이 얹지 않기 위해서**다. 거는 것을 권한다 — `main` 과 같은 방식이다.

### 화면 목록 (2026.8 · **S8-01 기준** · 8단계 착수)

> **여기 적힌 것은 '존재하는 화면' 이다.** 실제로 열리는지는 **`docs/ROUTES.md`** 가 든다
> (S0-04 · D-75 의 실동작 축). 이 표가 답하는 것은 "무엇이 있고 무엇이 필요한가" 이고,
> 그 화면이 계정별로 어떻게 열리는지는 그쪽 표에 있다.
> **전수 점검은 끝났다**(S0-04). 시드 9계정 + 비로그인 **열 축**으로 화면 97 개를
> 실제 Chrome 으로 전부 열었고 결과는 `docs/ROUTES.md` 에 있다. 다시 돌리는 방법도
> 거기 적혀 있다 — `npm run audit:screens` · `npm run audit:api` · `npm run audit:report`.
>
> **S0-04 가 다시 세었고 낡아 있었다.** T-00k 가 「실제 69 · 이 표 69」 로 적어 둔 뒤
> 6~8단계 화면이 들어와 **실제 97 · 이 표 87** 이 됐고, 표에 없는 화면이 **열** 이었다
> (`/admin/rules`·`/admin/ai-quality`·`/admin/cms`·`/admin/tickets`·`/admin/reviews`·
> `/admin/flags`·`/admin/ops`·`/vendor/reviews`·`/reviews/new/[bookingId]`·`/support`).
> **열을 이 표에 넣어 97 로 맞췄다.** 손으로 세지 말고 `npm run audit:routes` 를 쓴다 —
> 그것이 명세 §6·커버리지 표 D·실제 라우트를 한 번에 대조한다. 세는 방법:
>
> ```bat
> :: 실제 화면 라우트 수 (라우트 그룹 "(...)"는 URL 에 안 들어간다)
> node -e "const{readdirSync,statSync}=require('fs');const p=[];(function w(d){for(const e of readdirSync(d)){const f=d+'/'+e;statSync(f).isDirectory()?w(f):e==='page.tsx'&&p.push(f)}})('app');console.log(p.length)"
> ```

#### 지금 로그인 없이 열리는 화면 — **18개**

`/` · `/login` · `/explore` · `/explore/[vendorId]` · `/search` ·
`/prices/[region]/[category]` · `/qna/[vendorId]` · `/planners` · `/planners/[id]` ·
**`/planners/ranking`**(S6-06) ·
`/community` · `/community/[postId]` · `/guides` · `/guides/[slug]` ·
`/share/[token]` · `/rsvp/[token]` · `/tools/penalty` · `/design-system`(dev)

> **T-00h 는 이 목록을 '8개' 라고 적었고 실제로는 12개를 나열하고 있었다.** 그 뒤
> S7-04(`/tools/penalty`)·S7-10(`/guides`)·S7-09(`/rsvp/[token]`)가 더해져 **18개**다
> (제목은 18, 본문은 17 이라 적혀 있었다 — **S0-04 가 비로그인으로 97개를 전부 열어**
> 실제로 로그인을 요구하지 않는 것이 **정확히 이 18개**임을 확인했다).
> 판정 기준을 적어 둔다 — **`page.tsx` 가 `requireUser()` 를 부르지 않고**
> **`middleware.ts` 의 `PROTECTED_PREFIXES` 에도 걸리지 않는 것.**
>
> **`/vendor/invite/[token]` 은 여기 없다.** 페이지 자체는 로그인을 요구하지 않지만
> 미들웨어가 `/vendor` 접두어로 막는다 — 초대 수락에 **이메일 일치**가 필요하므로
> 계정이 있어야 하는 것이 맞고, 로그인 뒤 `?next=` 로 돌아온다(S2-09).

**나머지 79개는 로그인 뒤에 있다.** 그 79개도 S0-04 가 계정별로 전부 열었다 — 결과는 `docs/ROUTES.md`.

#### 화면에 들어가기 전에 필요한 것 (전제 조건)

계정만으로는 안 열리는 화면이 있다. **막히면 여기를 먼저 본다** — 대부분 "권한이 없다"
가 아니라 **선행 데이터가 없다**.

| 전제 | 무엇이 필요한가 | 없으면 어떻게 되나 | 걸리는 화면 |
|---|---|---|---|
| **업체 승인** | 시드 업체는 `pending` 으로 시작한다. `admin@local.test` 로 `/admin/vendors` 에서 승인해야 `active` 가 된다 | `is_active_vendor` 가 막아 **탐색·상세에서 안 보이고 채팅·상담이 안 열린다**(S4-01) | `/explore` 목록 노출 · `/explore/[vendorId]`(미승인은 404) · `/chat` 개설 · `/consultations` 신청 |
| **커플 생성(온보딩)** | `couple-a@local.test` 로 `/onboarding` 6문항을 끝내야 커플이 생기고 그 계정이 **owner** 가 된다 | 커플이 없으면 커플 스코프 화면이 **빈 상태 또는 리다이렉트**다 | `/home` · `/cart` · `/wishlist` · `/explore/compare` · `/checklist` · `/planner` · `/reports` · `/inquiries` · `/consultations` |
| **커플 연동** | `couple-a` 가 발급한 코드를 `couple-b@local.test` 가 수락. **또는** 이미 연동된 `couple-linked-a/b` 를 쓴다 | 배우자 공유(D-19)를 확인할 수 없다. `db:rls` 는 `couple-linked-*` 를 쓴다 | 장바구니 공유 표시 · 담은 사람 표시 |
| **커플 소유자(owner)** | 결제·서명·해지·에스크로는 **owner 1인만** 한다(D-32 · §3.9) | 배우자(partner)에게는 **RLS 가 아예 안 보여준다** — 권한 오류가 아니라 빈 결과다 | `/checkout/[bookingId]` · `/bookings/[id]/cancel` · `/bookings/[id]/escrow` |
| **업체 멤버 소속** | `/vendor/**` 는 `profiles.role` 이 아니라 **`vendor_members` 소속**으로 판정한다 | 소속이 없으면 업체 화면이 안 열린다. 시드가 `vendor@local.test`(owner)·`staff@local.test`(staff)를 넣는다 | `/vendor/**` 전부 |
| **업체 대표(owner)** | 가격·정산에 닿는 칸은 대표 전용이다(S2-07 · 0028) | `staff` 로 보면 **잠긴 칸** 또는 **빈 목록**이다(정산은 RLS 가 빈 목록을 준다) | `/vendor/settlements`(대표 전용) · `/vendor/products` 판매가 · `/vendor/settings` 대표 칸 · `/vendor/members` |
| **요율 1행 이상** | `commission_rates` 가 비면 계약 발행이 `CONTRACT_RATE_UNRESOLVED` 로 막힌다. `seed:accounts` 가 **로컬 데모 요율**을 넣는다 | 계약·결제·정산·해지 흐름이 **전부** 안 선다(FIX-11) | `/checkout/*` · `/bookings/*` · `/vendor/settlements` · `/admin/settlements` |
| **AI 상한 파라미터** | `ai.free_daily_turns`·`ai.session_token_cap`. `seed.sql` 은 **키만** 만들고 값은 `seed:accounts` 가 넣는다 | `/planner` 가 **"한도가 설정되지 않았다"** 로 열린다 — 고장이 아니라 결정이다(D-49 · FIX-21) | `/planner` |
| **계약·예약 행 존재** | ~~시드가 만들지 않는다~~ → **S5-12 가 채웠다**: 활성 계약 1 + 미납 회차 2(20/80)를 시드한다 | **진입 화면은 S5-10 이 세웠고 낼 회차는 S5-12 가 세웠다** — 그전까지 결제 화면은 **그릴 것이 없어** 쿠폰 자리까지 닿지 못했다. — `/me` → `/bookings` → `/bookings/[id]` 로 들어가면 다섯 화면을 URL 없이 열 수 있다. 시드가 예약 행을 넣으므로(`seed:accounts`) 이제 실제로 밟을 수 있다 | `/checkout/[bookingId]` · `/bookings/[id]/cancel` · `/bookings/[id]/escrow` · `/vendor/escrow` · `/vendor/cancellations` |
| **피처 플래그** | `feature_flags.community.enabled` | 꺼져 있으면 커뮤니티 화면이 **404**. **지금은 켜져 있다**(0041 · S7-16) | `/community` · `/community/[postId]` · `/community/write` |
| **운영자 역할** | `profiles.role ∈ {ops, admin}`. **RLS 가 아니라 서버 코드가 경계**다(아래 §접근 권한) | `/login?next=...&denied=1` 로 돌아간다 | `/admin/**` 전부 |
| **플래너 레코드** | `planners` 행 + 활성 위임. 시드가 `planner@local.test` 에 만든다 | `/pro` 가 등록 화면으로 열린다. **공개 승인을 누를 운영자 화면이 없다**(FIX-17) | `/pro` |
| **토큰·대화·글 행 존재** | ~~시드가 만들지 않는다~~ → **FIX-56 이 채웠다**: 채팅방 1(메시지 3) · 커뮤니티 글 2(댓글 2 · 업체 태그 1) · 공유 링크 2(살아 있는 것/거둔 것) · 하객 3(초대 토큰 1) · 업체 초대 1 · 태스크 4(간선 1) · 상품 옵션 2 · 가격 룰 2 · 상담 2 · 해지 1 | **그전까지 화면 다섯을 실제 데이터로 열어 본 사람이 아무도 없었다** — 없는 id 로만 열려 '못 찾음' 경로만 확인됐다(S0-04) | `/chat/[roomId]` · `/community/[postId]` · `/share/[token]` · `/rsvp/[token]` · `/vendor/invite/[token]` · `/checklist` · `/vendor/pricing` · `/vendor/cancellations` |
| **업체 승인 뒤에만 열리는 것** | `community_tag_vendor_guard()` 는 **승인된 업체만** 태그를 허용한다. 데모 업체는 `pending` 으로 시작하므로 시드는 **표본 업체**에 태그를 붙인다 | `/vendor/community` 가 **빈 상태**이고 '업체 답변' 배지(F-V-18)가 안 뜬다. `/admin/vendors` 에서 승인한 뒤 `npm run seed:accounts` 를 다시 돌리면 태그가 옮겨 온다 | `/vendor/community` · `/community/[postId]` 의 업체 답변 배지 |

| 경로 | 필요한 계정 | 내용 | 태스크 |
|---|---|---|---|
| `/` | **없음(비로그인 가능)** | **랜딩.** 가치 제안 · 가격 표시 규칙 예시 · 광고 반영 없음 고지 · 업체 입점 진입. **완전 정적**이라 로그인 여부로 화면이 갈리지 않는다. `/robots.txt`·`/sitemap.xml` 도 함께 | S3-10 |
| `/login` | 없음 | 이메일·비밀번호 로그인·회원가입 + **소셜 버튼 자리**(공급자 키 등록 전까지 비활성 — S3-01b). ~~⛔ 지금 여기서 막힌다 — FIX-24~~ **해소**(S8-01) · S0-04 가 9계정 전부로 통과를 확인했다 | S2-01, S3-01 |
| `/onboarding` | consumer(`couple-a@local.test`) | 온보딩 6문항(한 화면 한 질문·진행률·뒤로가기·**미정 허용**) + **배우자 초대**(코드 발급/수락) + 최근 활동. `couple-b@local.test` 로 코드를 수락하면 같은 정보를 공유하는 것을 볼 수 있다. **여기를 지나야 커플이 생긴다** — 커플 스코프 화면 전부의 전제다 | S3-01 |
| `/design-system` | 없음 | 디자인 시스템 카탈로그. **개발 전용 라우트** | S1-01~03 |
| `/explore` | **없음(비로그인 가능)** | 업체 탐색. 지역·예산·하객수·날짜·스타일 필터 + **정렬 기준 배지**(기본 가격 낮은 순). 0건이면 **어느 조건을 풀면 몇 건인지** 알려 준다 | S3-03 |
| `/explore/[vendorId]` | **없음(비로그인 가능)** | 업체 상세. 총액·포함 항목·**추가금 사전표**를 한 화면에서 본다. 예식일을 고르면 그날 **잔여 자리 + 최종가·할인 사유·계산 근거**. 미승인 업체는 404 | S3-03 |
| `/search` | **없음(비로그인 가능)** | **조건 검색**(F-C-30). 자연어+조건 혼합 입력 → **파싱 결과 칩**(칩마다 지우기·출처 표시·직접 고치기) · **못 읽은 조각과 그 이유** · **랭킹 기준 배지 + 가중치 펼침**. 키가 없으면 **룰 파서만으로** 선다(D-44 · 랜딩·사이트맵에 진입이 있다). **이 표에 빠져 있던 화면이다 — T-00h 가 넣었다** | S7-02 |
| `/vendor/apply` | vendor | 입점 신청 + 심사 상태 추적 | S2-01 |
| `/vendor/profile` | vendor | 프로필·미디어·변경 이력 + **스타일 태그**(탐색 스타일 필터의 데이터 출처) | S2-02, S3-03 |
| `/vendor/products` | vendor | 상품 목록 | S2-03 |
| `/vendor/products/new` | vendor | 상품 등록(총액 강제·예상 정산액) | S2-03 |
| `/vendor/products/[id]` | vendor | 상품 상세·수정·게시 + **추가금 사전 등록** | S2-03, S2-04 |
| `/vendor/bookings` | vendor | **예약·계약 보드**(F-V-08 · S5-10). 갈래 넷 — 승인 대기 / 계약 발행 대기 / 진행 중 / 종료. **0건인 갈래도 줄을 남긴다.** 승인은 계약 확정과 **다른 사건**이며(D-151 · D-36) 거절에는 사유가 필수다. 결정은 되돌릴 수 없다 | **S5-10** |
| `/vendor/members` | vendor(owner)·staff | 멤버 목록·초대·권한 변경. **staff 로 로그인하면 제한을 눈으로 확인**할 수 있다 | S2-07 |
| `/vendor/inventory` | vendor·**staff 가능** | 월 달력 + 반복 규칙·CSV·블록 처리. CSV 형식은 `docs/INVENTORY_CSV.md` | S2-05 |
| `/vendor/pricing` | vendor(owner 편집·staff 조회) | 시즌·요일·리드타임·잔여율 룰 + **시뮬레이션**(적용 근거·가드 표시) | S2-06 |
| `/vendor` | vendor·staff | 대시보드. **지금 할 일**(추가금 미확정·재고 없음·프로필 누락)과 현황 요약 | S2-08 |
| `/vendor/stats` | vendor·staff(정산은 owner) | 성과 통계. 아직 못 세는 지표는 **'집계 대상 없음' + 연결 예정 태스크** 표시 | S2-08 |
| `/cart` | consumer | 장바구니. **최대 5개**(`?cart=<순번>` 탭 전환) · 이름 인라인 편집 · **예산 대비 여유·초과** · **카테고리 채움**(빈 칸은 '아직 안 담음', 비면 '미완성 총액') · 생성·치우기·**복제** · 항목 **이동·복사** · 업체별 묶음 · 합산 총액 · 항목별 플래너 토글(선택 즉시 총액 반영) · 담은 사람 표시. `couple-linked-b` 로 로그인하면 같은 장바구니들이 보인다 | S3-05, **S3-12** |
| `/home` | consumer | **소비자 홈**. D-day(예식일 미정이면 숫자 대신 '정하기') · 지금 할 일 3건 · **예산 게이지**(S7-07 · 총예산이 미정이면 **그리지 않는다**) · 담아 둔 것 요약 · **다음 할 일 3건**(`/checklist` 와 같은 컴포넌트 · S7-19) · **클리어 진입**(S7-06) · 준비 중 기능 자리(0이 아니라 '아직'). 로그인하면 여기로 온다 | S3-11, S7-06, S7-08, **S7-07** |
| `/planner` | consumer | **AI 플래너 '클리어'**. 대화 목록(`?c=<id>`) · 스트리밍 답변 · **툴 결과 카드**(정렬 기준 배지) · AI 고지 상시. **키가 없는 로컬에서는 `rules_only` 모드**로 열린다 — 조건이 읽히면 업체를 찾아 카드로 보여주고, 못 읽으면 어떻게 적으면 되는지 알려준다. 상한(`ai.*`)이 비어 있으면 입력창이 안 뜬다 | S7-06 |
| `/reports` | consumer | **계약서 검토 목록**. 상태·위험 점수·**파기 예정 시각**. 원문은 분석이 끝나면 바로 지워지므로 목록에는 리포트만 남는다 | S7-03 |
| `/share/[token]` | consumer | **공유받은 검토 결과**. **로그인 없이 열린다** — 토큰을 가진 것이 곧 권한이다. **뷰 전용·마스킹 상태**이고 고칠 수단이 없다. **협상 문구는 빠진다**(당사자가 아니다). 닫힌 링크는 **없음·만료·거둠**을 각각 다른 문장으로 말한다. **하단 탭을 숨긴다** — 받는 사람에게는 전부 로그인 벽이다. 색인 차단(`robots: index:false`) | **S7-12** |
| `/estimates` | consumer | **견적 비교**. 업체가 표준 양식으로 보낸 견적 중 **2~5개를 골라** 표준 항목으로 맞춰 나란히 놓는다. **업로드 슬롯이 없다** — 자유 양식 견적이 존재하지 않고(F-V-07) 파싱 경로가 안 열렸다(D-56). **실총액은 필수 옵션까지**이고 선택 옵션은 따로 알린다. **빈 칸은 0이 아니라 '없음'**, 카테고리가 섞이면 **총액 우열을 정하지 않는다**. 합계 불일치·만료·확인 필요를 플래그로 짚는다. 비교표를 **남기면 공유할 수 있다** | **S7-05** |
| `/vendor/compliance` | vendor | **약관 자가 진단**(F-V-10). 붙여넣은 약관을 **검출 룰 20종**(T-04)으로 검사한다 — **AI 를 쓰지 않는다**(같은 문서에 같은 답이 나와야 배지가 우연이 아니다). **원문을 저장하지 않고** 걸린 항목과 인용 조각만 남기며, 인용은 저장 직전 마스킹하고 **가리지 못하면 인용을 버린다**. 걸린 항목마다 **무엇이 담겨야 하는지**를 적는다(조항 문안을 대신 쓰지 않는다). 통과하면 **투명 계약 배지**가 붙고 약관이 나빠지면 **회수된다**(DB 트리거). 진단 전에는 '0건' 이 아니라 **'아직 세지 않았다'** 고 적는다. '참고 정보이며 법률 자문이 아님' 상시 노출 | **S7-13** |
| `/guests` | consumer | **하객·좌석**(F-C-22). 명단 CRUD · 참석 응답 집계 · **테이블 단위 좌석 초안** · **답례품 수량**. 수량은 **확정·상한·미응답 셋**으로 낸다 — 하나의 숫자로 답하면 사용자가 확정으로 읽는다. 예상 하객 수가 없으면 **0과 견주지 않는다**. **초대 링크는 예식일이 있어야** 만들어진다(만료를 예식일이 정한다). 하객 이름은 **커플과 위임 플래너만** 본다 | **S7-09** |
| `/rsvp/[token]` | **없음(비로그인 가능)** | **하객 참석 응답**. 하객마다 다른 토큰이며 본인 한 줄만 열린다 — **이름을 고칠 수 없고** 참석 여부와 인원만 답한다. 예식일이 지나면 닫히고 **커플이 거두면 즉시 404** 다(no-store). **noindex** — 토큰을 가진 것이 곧 권한이라 색인되면 그 자체가 유출이다. 하단 탭을 숨긴다 | **S7-09** |
| `/guides` | **없음(비로그인 가능)** | **가이드 목록**(F-C-24). 유형 셋으로 묶는다(가이드·가격 리포트·용어사전). **가격 리포트는 글이 없고 그 자리에 왜 없는지를 적는다** — 참가격 표본이 부족하다(S3-08 · S8-10 대기). 쿠키를 읽지 않아 **정적으로 생성**되며 진입은 **랜딩 상단**이다(하단 탭이 아니다 — 비로그인 화면이다) | **S7-10** |
| `/guides/[slug]` | **없음(비로그인 가능)** | **가이드 본문**. 목차·도구 CTA·JSON-LD. **본문은 블록 구조로 그린다** — HTML 문자열을 만들지 않으므로 본문에 `<script>` 가 있어도 글자로 보인다(살균기가 필요 없다). **CTA 는 레지스트리의 실재 경로만**이고 로그인이 필요하면 미리 적는다. 없는 글·한글 슬러그는 **404** | **S7-10** |
| `/membership` | consumer | **멤버십 구독**(F-C-19). 지금 내 등급·남은 기간과 **혜택 넷의 실제 상태**를 적는다 — **지금 갈리는 것은 AI 대화 턴 하나**이고 나머지는 "지금은 차이 없음"·"아직 없는 기능" 이라고 화면이 그대로 말한다("멤버십이면 무제한" 만 적으면 무료에 제한이 있는 줄 안다 · D-95). **가격이 미설정이면 0원을 그리지 않고 버튼을 열지 않는다**(O-17 · D-96). 결제는 **스텁**이며 프로덕션에서 거부한다(D-28). **해지는 지금 끊지 않는다** — 남은 기간은 그대로 쓴다. 진입은 **`/me`**(하단 탭 다섯 칸이 찼다) | **S7-11** |
| `/tools/penalty` | consumer | **위약금 시뮬레이터**. **로그인 없이 열린다** — 계산이 커플 데이터를 읽지 않는다(저장만 로그인+커플). 카테고리·총액·계약금·예식일·취소 시점·계약서 위약 조건 → **기준 대비 막대**·초과분·근거 조항·**이의 제기 문구 복사**. `penalty_rules` 가 비어 있어 **가정치로 계산한다는 사실을 결과보다 먼저** 적는다. **계산된 0원**(계약금 반환 구간)과 **기준 미설정**을 가른다. 진입은 `/reports` | **S7-04** |
| `/budget` | consumer | **예산 배분·추적**. 총예산(**미정이 0이 아니다** — 지우면 미정으로 돌아간다 · 장바구니 기준선과 **같은 값**) · **배분 도넛**(상위 5 + 기타 + 미배정 · 새 색을 만들지 않았다) · 카테고리 13종(계획·계약·지출·남음) · **참가격 권장**(기준이 없는 카테고리는 **빈칸**이지 0원이 아니다) · 실지출 등록·삭제 · **초과 경고 3종**(총액·계획 합·카테고리). **확정된 예약은 자동으로 잡힌다** | **S7-07** |
| `/checklist` | consumer | **일정·준비 순서**. 위는 **표현 넷 토글**(A 역산 타임라인 **기본** · B 카테고리 게이지 · C 다음 할 일 · D 단계·선행 편집), 아래는 **목록에서 고치기**(자동 생성 19종 + 순서 16개 · 카테고리 필터 · **캘린더로 기한 변경** · 완료). 넷이 **같은 조회**를 쓴다. `waiting` 은 **배지일 뿐 잠금이 아니다** — 어느 표현에서도 회색으로 칠하지 않고 완료 버튼이 살아 있다 | S7-08 · **S7-19** |
| `/community` | **없음(비로그인 가능)** | **커뮤니티 목록**. 게시판 탭 3(자유·경험담·질문) · 정렬 2(최신·댓글 활동) · **정렬 기준 배지**. **지금은 열리지 않는다** — `feature_flags.community.enabled` 가 꺼져 있어 404 다(S7-17 모더레이션 뒤 개방) | S7-15 |
| `/community/[postId]` | **없음(비로그인 가능)** | **글 상세**. 업체 태그는 **'미검증 경험담'** 라벨과 함께 점선 영역에 둔다 · 댓글 2단 · 좋아요·스크랩·신고. 쓰기는 로그인 | S7-15 |
| `/community/write` | consumer | **글쓰기**. 본문에 등록 업체명이 보이면 **태그로 붙일지 제안**한다(막지 않는다) · '완전 차단을 약속하지 않는다' 를 화면이 적는다 | S7-15 |
| `/admin/community-reports` | admin·ops | **커뮤니티 모더레이션**. 신고 큐(오래된 것부터)·처리 이력 · 조치 3(비공개·다시 공개·조치 없음, **삭제는 없다**) · **사유 필수** · 어뷰징 신호(판정 아님 · O-14). **커뮤니티가 닫혀 있어도 열린다** — 이미 접수된 신고는 처리돼야 한다 | S7-17 |
| `/vendor/community` | vendor·**staff 가능** | **커뮤니티 태그**. 자사가 태그된 글 · **공식 답변 1회**(고치기는 가능) · 부당 게시물 신고. **본문 수정·삭제 수단이 없다** — 내리는 것은 운영자의 일이다(D-24) | S7-16 |
| `/reports/upload` | consumer | **계약서 올리기**. 파기·마스킹·형식 고지 + **동의 체크**(없으면 서버가 422). 파일은 **서명 주소로 Storage 직행**이라 우리 서버를 지나지 않는다. **지금은 `.txt` 만 읽는다**(PDF·OCR 은 어댑터 자리 · D-56) | S7-03 |
| `/reports/[id]` | consumer | **리포트 상세**. 등급별 조항 카드·근거·요청 문구 복사·**고지 상시**. 분석 중이면 폴링하며, 그 폴링이 **끊긴 실행을 되살리는 계기**다(D-59) | S7-03 |
| `/consultations` | consumer | **상담·탐방 예약**. 예정/지난 일정 · 보증금 상태 · **이행 확인 응답** · 취소(무료 기한 전인지 미리 알려준다). 보증금이 플랫폼 수수료가 아니라 보관금임을 상시 고지(D-24) | S4-07·S4-08·S4-09 |
| `/vendor/consultations` | vendor·**staff 가능** | 신청 승인·거절 · 확정 일정 · **이행 확인**(= 노쇼 신고). 별도 노쇼 신고 버튼을 두지 않는다 — 일방 주장이 §3.11 의 양측 대조를 건너뛰지 않게 | S4-07·S4-09 |
| `/vendor/settings` | vendor(대표 편집·staff 조회) | **알림·연동 설정**. 수신 대상(전원/담당자 우선/지정) · 조직 채널 · **영업시간**(SLA 판정에는 안 쓴다) · 빠른 답변·견적 **템플릿** · **멤버 초대**. `staff@local.test` 로 로그인하면 대표 전용 칸이 잠긴 것을 확인할 수 있다 | S4-14 |
| `/vendor/invite/[token]` | **없음(비로그인 가능)** | **초대 수락**. 어느 업체가 어떤 권한으로 불렀는지 보여주고 로그인으로 보낸다. 아직 멤버가 아니라 AdminShell 을 쓰지 않는다. 이메일은 마스킹돼 보인다 | S2-09 |
| `/vendor/availability` | vendor·**staff 가능** | **상담 가능 시간대**(요일·시간·슬롯 길이). 겹치면 DB(EXCLUDE)가 거부한다. 날짜 예외(휴무)는 재고 캘린더의 블록 처리 | S4-06 |
| `/inquiries` | consumer | **문의함**(1:N 표준 문의). 업체별 응답 상태·**미응답/거절 구분**·SLA 잔여·받은 견적(상한 대비 할인액 표시). 상단에 **세 경로 안내**(문의·채팅·게시판이 무엇이 다른지) | S4-12 |
| `/qna/[vendorId]` | **없음(비로그인 가능)** | **문의게시판**. 공개/비공개 선택 · **쓰기 전에 유사 질문 노출** · 업체 답변. 공개글은 로그인 없이 읽는다 | S4-05 |
| `/vendor/inquiries` | vendor·**staff 가능** | **표준 견적 폼**. 등록된 게시 상품·추가금만 고를 수 있고 **항목 이름 칸·첨부·총액 입력이 없다**. SLA 타이머 · 거절 사유 코드 | S4-12 |
| `/vendor/qna` | vendor·**staff 가능** | 문의게시판 관리. 미답변 큐 · 답변 작성·수정 · **공개 설정은 내리는 방향만** | S4-05 |
| `/chat` | consumer | **대화 목록**. 업체명·마지막 메시지·안읽음 배지, 최근 순. 실시간이 끊기면 폴링으로 내려가고 그 사실을 화면에 적는다 | S4-04 |
| `/chat/[roomId]` | consumer | **대화방**. 읽음 표시(내가 보낸 것에만) · 첨부(이미지·PDF ≤20MB, 서명 URL) · **회수**(본문은 남고 상대 화면에서만 가려진다) · 상담 일정 제안 카드 자리(발송은 S4-07) | S4-04 |
| `/vendor/chat` | vendor·**staff 가능** | **채팅 응대 인박스**. 미응답 우선 정렬 · 담당자 배정 · **응답 SLA 타이머**(눈금은 `app_settings.chat.sla_response_minutes`) · 빠른 답변 템플릿 | S4-04 |
| `/notifications` | consumer | **알림센터**. 토픽별 목록(안 읽음·읽음·보내지 않음 구분) · 읽음/모두 읽음 · **토픽×채널 수신 설정**. 앱 알림함은 끌 수 없다 | S4-13 |
| `/me` | consumer | **마이페이지**. 프로필(연락처는 해시라 '등록됨'만 표시) · 결혼 준비 정보 수정(온보딩과 같은 스텝퍼) · 동의 이력(철회 가능 여부 구분) · 커플 연동 해제 · **계정·데이터 삭제 요청과 거두기** | S3-09 |
| `/prices/[region]/[category]` | **없음(비로그인 가능)** | 지역·카테고리 가격 분포(p25·p50·p75). **출처·표본수·수집일 상시 표기** · 표본 5곳 미만이면 값을 만들지 않는다. **지수를 채우는 배치는 S8-10** 이라 지금은 대부분 '표본 부족' 으로 보인다 | S3-08 |
| `/explore/compare` | consumer | **장바구니 기반 비교표**. **층위 전환**(`?mode=carts` 장바구니끼리 / `?mode=items` 담은 항목끼리 — 장바구니가 둘 이상이면 조합끼리가 기본) · 조합끼리는 총액·예산 대비·카테고리별이고 **같은 줄은 접힌다** · 담은 카테고리가 다르면 '가장 낮은 총액'을 정하지 않는다 · 항목끼리는 카테고리별 묶음 · **실총액 낮은 순** · 플래너 기준 전환(표시만) · 비교할 항목 고르기. 375px 에서 표가 가로 스크롤되고 축 이름 열이 고정된다 | S3-07, **S3-12** |
| `/wishlist` | consumer | 찜. **담은 시점 가격 대비 변동**(오름/내림/변동 없음/확인 불가) · 장바구니로 옮기기(**장바구니가 둘 이상이면 어디로 보낼지 고른다**) | S3-05, S3-12 |
| `/coupons` | consumer | **내 쿠폰**(F-C-35 · S5-12). 받은 쿠폰·조건·기한. **여기서는 사용 가능 여부를 판정하지 않는다**(D-157) — 쿠폰마다 최소 결제 금액이 달라 어떤 결제냐에 따라 답이 바뀜다. 진입은 `/me` | **S5-12** |
| `/bookings` | consumer | **내 예약 목록**(§6.2 신설 제안 · S5-10). 상세만 있으면 **id 를 아는 사람만 열 수 있다.** 진입은 `/me` — 하단 탭은 다섯 칸이 찼다(D-55) | **S5-10** |
| `/bookings/[id]` | consumer | **예약 상세**(F-C-14·15). 상태 타임라인·회차별 금액·기한·환불 규정과 **다섯 진입점**(계약·결제·해지·안전거래·후기). 그 다섯은 라우트가 실재하는데 **이 화면이 없어 도달 불가였다**(FIX-25 계열). **막힌 문도 이유와 함께 그린다** | **S5-10**(S5-06 이 남긴 자리) |
| `/checkout/[bookingId]` | consumer(**커플 소유자만**) | **회차 결제**. 이번 회차 금액을 가장 크게, **계약 총액·이미 낸 금액·낼 뒤 남는 금액을 같은 블록에서**(D-18) · **회차 계획 전체**를 사유와 함께(못 내는 회차도 감추지 않는다) · **쿠폰 영역**(S5-12 가 열었다) — 쓸 수 있는 쿠폰을 고르고 **못 쓰는 쿠폰도 사유와 함께** 보인다(F-C-36). **금액을 보내지 않고 발급분 id 만 보낸다**(D-155) · 사용 처리는 **결제가 승인되는 순간**에 일어난다(D-154) · **결제 전 확인 2건**(회차 조건·취소 환불 규정, 동의 기록이 남는다) · 스텁 고지 · 중개자 고지(D-24). 배우자에게는 **RLS 가 안 보여준다**(§3.9 결제는 owner 조건) | **S5-06** |
| `/bookings/[id]/cancel` | consumer(**커플 소유자만**) | **계약 해지 요청**. 요청 버튼보다 **위에** 예상 위약금과 **산정 근거**(취소 시점·적용 구간·기준 출처·예식일까지 남은 일수)를 둔다 — 모르고 취소하게 두지 않는다. 귀책은 **의견**으로 고른다(그것만으로 위약금이 0 이 되지 않는다). 진행 중이면 양측 확인 현황을 보여준다. 가정치 룰이면 그 사실을 노출한다(§7.7) | **S5-08** |
| `/bookings/[id]/escrow` | consumer(**커플 소유자만**) | **안전거래**(F-C-16). 맡겨진 잔금·양측 이행 확인·기한·조율 결과. **"플랫폼이 돈을 받는다" 로 읽히지 않게** 주어가 금액이고 플랫폼은 **맡아 둔다**(D-24 · BrokerNotice 상시). **O-03 대기라 실예치 전이라는 사실을 맨 위에 고지**한다 — 자금 보관은 결제보다 법적 요건이 무겁다 | **S5-09** |
| `/vendor/escrow` | vendor·**staff 가능** | **안전거래 이행 확인**. 업체도 확인 당사자다 — 확인이 늦으면 릴리즈도 정산도 늦어진다. 금액은 서버가 정하고 화면은 "이행됐다/아니다" 만 받는다 | **S5-09** |
| `/planners` | **없음(비로그인 가능)** | **플래너 마켓**(F-C-18). 정렬 3종(계약 실적·경력·최근)과 카테고리 필터 + **정렬 기준 배지 상시 노출** — "추천"·"프리미엄" 정렬이 **없다**(§2.2 가 업체 목록에 세운 규칙을 그대로 적용). 후기는 0이 아니라 **"아직 세지 않아요"**(S8-11 대기) | **S6-02** |
| `/planners/[id]` | **없음(비로그인 가능)** | 플래너 상세. 소개·경력·카테고리·활동 지역·계약 실적. **요금을 보여주지 않는다** — 요율은 `planner_fee_rates` 가 갖고 계약 확정 시 스냅샷된다(D-16). **S6-04 가 위임 진입을 실제 경로로 이었다** | **S6-02** · **S6-04** |
| **`/planners/ranking`** | **없음(비로그인 가능)** | **순서 기준 공개**(D-25). 순서의 근거만 보이고 **목록은 그리지 않는다** — 같은 지표로 목록을 또 그리면 그것은 같은 목록이다(D-174). 다섯 지표를 다 싣고 못 세는 이유를 둘로 가른다. **쿠키를 읽지 않아 정적으로 굳는데 그것이 의도한 것**이다(내용이 전부 코드 상수다) | **S6-06** |
| **`/planners/scopes`** | 커플 | **플래너 이용 범위**(F-C-31). 카테고리 여덟을 **안 고른 것까지** 보이고 담긴 항목의 판매가·붙는 수수료를 함께 적는다. **요율이 없으면 0원이라 적지 않는다**(0원과 '기준 모름' 은 화면에서 겹쳐 읽힌다). **집행은 계약 발행**이며(D-168) 화면이 그 사실을 적는다. 진입은 `/cart` 와 위임 관리 | **S6-03** |
| **`/planners/[id]/delegate`** | 커플(**대표만 폼**) | **위임 제안**(F-C-18). 범위 11종을 **무엇이 열리는지와 함께** 고르고 기간을 정한다 — 목록은 지어낸 것이 아니라 `has_planner_scope` 를 부르는 **정책에서 뽑았다**(D-167). **막아 둔 범위**(채팅·결제·계약·문서·쿠폰)를 같은 화면에 이유와 함께 보인다. **제안만으로는 아무것도 열리지 않는다**(D-165) | **S6-04** |
| **`/planners/delegations`** | 커플 | **위임 관리**. 국면을 **계산해** 보인다 — `active` 인데 기간이 지났으면 '열람 중' 이 아니라 **'기간 종료'** 다(만료를 저장하지 않는 이유 · D-166). 거두기 전에 무엇이 바뀌는지 먼저 말하고, 특히 **카테고리 선택은 그대로**라는 것을 적는다(D-43) | **S6-04** |
| `/pro` | **planner**(`planner@local.test`) | **플래너 콘솔 — 내 프로필**. 등록·수정·**공개 신청**·공개 내리기. **요금 칸이 없고** 그 이유를 화면이 먼저 말한다. 버튼이 "공개" 가 아니라 **"공개 신청"** 인 이유는 자가 공개를 DB 트리거가 막기 때문이다(승인 화면은 **FIX-17**). `/planner`(단수)는 §6.2 가 **AI 플래너 채팅**에 배정한 경로라 접두어를 갈랐다 | **S6-02** |
| **`/pro/engagements`** | **planner** | **받은 위임**. 고객이 제안한 범위·기간을 보고 **수락·거절**한다. **수락 전에는 고객이 누구인지 열리지 않는다** — `couples` 는 활성 위임에만 열리는 표이고, 이름을 보여 주려면 제안 자체가 열람 경로가 되어야 한다. 할 수 있는 일은 둘뿐이며 **범위를 넓히는 칸이 없다**(스스로 넓히면 자기 수수료를 늘리는 행위다) | **S6-04** |
| **`/pro/settlements`** | **planner** | **내 정산**(D-21). 합계 넷을 따로 적고 **'받을 수 있음' 과 '받았음' 을 합치지 않는다** — 합치면 이미 받은 줄 알고 입금을 기다리지 않는다. 국면은 **시계로 계산**하므로 배치가 하루 안 돌아도 사실을 말한다. 시드는 **두 갈래를 다 덮는다**(유예 지난 것 1 · 유예 중 1) | **S6-05** |
| `/vendor/cancellations` | vendor·**staff 가능** | **해지 요청 응대**. 동의 여부와 **귀책 의견을 따로** 받는다 — "해지에는 동의하지만 우리 잘못은 아니다" 를 표현할 수 있어야 한다. 산정 근거를 업체에도 보여준다(무엇에 동의하는지 모르면 그 동의는 증적으로서 약하다). 기한 내 무응답이면 조율로 넘어간다 | **S5-08** |
| `/admin/penalties` | admin·ops | **위약금 조율 큐**(F-A-17). 양측 확인이 갈리거나 기한이 지난 건이 쌓인다. 운영자가 정하는 것은 **귀책**이고 금액은 서버가 산정한다 — **금액 입력칸이 없다**. **사유 없이 종결할 수 없다**(D-24) | **S5-08** |
| `/vendor/coupons` | vendor | **자사 쿠폰 발행·현황**(F-V-19 · S5-13). 발행 조건·정액/정률·상한·기간·수량 · 발급·사용 현황 · **정산 차감액은 대표 전용**(§3.9 · D-160) — `staff` 로 보면 금액이 **0 이 아니라 '못 봄'** 으로 뜼고, 그것은 RLS 가 행을 안 주기 때문이다. **발급이 시작되면 할인 조건이 얼어붙는다**(D-159). **지금은 쿠폰이 자동 발급되지 않는다**(FIX-46) — 화면이 그 사실을 적는다 | **S5-13** |
| `/vendor/settlements` | vendor(**대표 전용**) | **정산**. 거래 총액·수수료·쿠폰 차감·상계를 나란히 두고 **가장 큰 글씨는 실제 지급액**(D-18) · 건별 명세에 **적용 요율(bp)** 표시 + "계약 확정 시점 스냅샷이라 지금 요율과 다를 수 있다" 고지(D-16) · **다음 정산에서 차감 예정**인 상계 · 세금계산서 **자료 CSV**. **수수료 기준 미결이면 '설정 대기'** 로 보인다(경고색이 아니라 안내색 — 장애가 아니다). staff 에게는 **RLS 가 빈 목록**을 준다 | **S5-07** |
| `/admin/coupons` | admin·ops | **플랫폼 쿠폰 관리**(F-A-19). 발행·중단 · 소진 현황 · **비용 집계**(30일·누적) — **전액 플랫폼 손익**이며 업체 정산에 닿지 않는다. **업체 쿠폰은 여기서 발행하지 않는다**(T-00e) — 그 경계는 화면이 아니라 **정책**이 지킨다(D-163). **대상 세그먼트는 미구현**(D-164 · FIX-46) | **S5-14** |
| `/admin/settlements` | admin·ops | **정산 집행**(F-A-11). 업체별 집계 · 확정 · 지급 · 재계산. **금액 입력칸이 없다** — 운영자가 정하는 것은 '언제'이지 '얼마'가 아니다. 업체 이의 제기 메모가 함께 보인다 | **S5-07** |
| `/admin/commission-rates` | admin·ops | **요율 관리**(F-A-15). 전역·카테고리·업체별 요율 등록 · **기간 이력** · **적용 요율 시뮬레이터**(계약 발행과 같은 규칙으로 조회). **요율은 삭제하지 않고 종료**한다(D-23) · 소급되지 않는다는 고지가 저장 버튼 옆에 있다(D-16) · 값 범위를 화면이 강제하지 않는다(O-02). **⚠ `ADMIN_NAV` 에 없어 URL 을 직접 쳐야 한다**(FIX-25) | **S5-03** |
| **`/admin`** | admin·ops | **지표 대시보드**(F-A-07). KPI 13종 + 단계별 퍼널 7단계 · 기간 7/30/90일. **못 세는 값을 0으로 적지 않는다** — 수수료 수익은 `기준 미확정`(O-15)이고 노출·AI 비용은 `집계 대상 없음`(S8-05·S8-04). 측정된 값에는 **무엇을 세었는지**가 숫자 밑에 붙는다. 차트 라이브러리 없이 CSS 막대다 | **S8-01** |
| **`/admin/prices`** | admin·ops | **가격 큐레이션·이상 탐지**(F-A-02·F-A-14). 지수 현황(표본 부족 구간은 **0원이 아니라 '값을 만들지 않음'**) · 원천 표본 검증·제외(**사유·처리자 필수**) · 재계산 · 이상 탐지 큐. **임계값이 O-19 미결이라 탐지를 돌리지 않고** 화면이 그 사실을 말한다. 판정이 아니라 큐이며 자동 제재가 없다 | **S8-10** |
| **`/admin/disputes`** | admin·ops | **분쟁 조율**(F-A-12·F-A-16). 네 출처(예약 분쟁·노쇼 보증금·취소 위약금·안전거래 이의)를 **한 큐**로 본다. **상태 어휘는 합치지 않는다** — 출처 배지 + 원 상태가 함께 뜨고 집행은 각 도메인 규칙이다(D-121). 조치 넷은 전부 *제시·기록*이며 **판정 버튼이 없다**(D-24). 합의는 **양측 동의 필수** · 불성립 뒤 절차는 약관 소관 | **S8-03** |
| **`/admin/consultation-disputes`** | admin·ops | **노쇼 분쟁**(F-A-16). 통합 큐의 **필터된 입구**다 — `?source=consultation` 으로 보낸다. 목록을 다시 그리지 않는다 | **S4-10 / S8-03** |
| **`/admin/privacy`** | admin·ops | **개인정보 감사**(F-A-08). 파기 배치 이력 · **잔존·실패 경보**(주의/즉시확인 2단) · 삭제 요청 SLA 큐(처리 3종 · **사유 필수**) · 마스킹 차단 로그. **문서를 한 건씩 보여주지 않는다** — `storage_path` 는 §5.3 금지 항목이라 개수와 경과 시간만 낸다. 처리 기한은 **O-18 미결**이라 판정하지 않는다 | **S8-04** |
| **`/admin/audit`** | admin·ops | **감사 로그·증적 타임라인**(F-A-09). `audit_logs`(행위)와 `entity_events`(전이)를 **한 줄로** 세운다 · 행위자·액션·대상 필터 · **CSV 내보내기**. **값이 아니라 바뀐 칸**을 보여주고 연락처·경로·토큰 같은 칸은 `(가림)` 이다(§7.3). 조율 결정의 **근거 이벤트 id** 를 함께 낸다 | **S8-02** |
| `/admin/vendors` | admin·ops | 입점 심사 큐·승인/보완/반려. **여기서 승인해야 시드 업체가 `active` 가 되고** 탐색·채팅·상담이 열린다 | S2-01 |
| **`/admin/rules`** | admin·ops | **룰·프롬프트 콘솔**(F-A-03). 검출 룰 목록·버전, 프롬프트 배포·롤백. AdminShell · 상태 3종 | S8-06 |
| **`/admin/ai-quality`** | admin·ops | **AI 품질·비용**(F-A-04). 실패율·폐기율·지연·비용, 샘플 검수 큐, 오탐 신고 진입점. **토큰 단가가 미결이라 비용은 '기준 미결'** 로 적는다(O-21) | S8-07 |
| **`/admin/cms`** | admin·ops | **콘텐츠**(F-A-05). 글 목록·에디터·판본·공개 시각·SEO 필드 | S8-08 |
| **`/admin/tickets`** | admin·ops | **CS·신고 큐**(F-A-06). 담당자 배정·처리 이력. **커뮤니티 신고와 합치지 않는다**(D-142) — 서로의 열린 건수만 가리킨다 | S8-09 |
| **`/admin/reviews`** | admin·ops | **후기 관리**(F-A-13). 검증 상태·어뷰징 탐지 큐·비공개 처리. **버스트 임계는 미결이라 '기준 미결'** 이다(O-20) | S8-11 |
| **`/admin/flags`** | admin·ops | **피처 플래그**(F-A-10). 부분 공개·긴급 롤백. **캐시하지 않는다** — 캐시된 스위치는 스위치가 아니다(FIX-22 계열). 목록 자체가 로드맵이라 운영자만 본다(D-15) | S8-12 |
| **`/admin/ops`** | admin·ops | **운영 상태**(§7.4 · **§6.4 신설 제안**). 배치 상태를 넷으로 가른다(등록 안 됨 / 실행 기록 없음 / 성공 / 실패). **경보를 보내는 시늉을 하지 않는다**(D-147) | S8-13 |
| **`/vendor/reviews`** | vendor·staff | **후기·평판**(F-V-11). 열람·답변·신고. 커뮤니티 태그와 다르다 — 이쪽은 **거래가 확인된 평가**다 | S8-11 |
| **`/reviews/new/[bookingId]`** | couple(owner) | **후기 작성**(F-C-17). 항목별 평점·실지출 공개 선택. **자격이 없으면 막힌 이유를 적는다** | S8-11 |
| **`/support`** | 로그인 | **문의·신고 접수**(F-A-06 접수 면 · **§6.2 신설 제안**). `/admin/tickets` 큐에 들어갈 티켓을 사용자가 만드는 자리다 — **접수 경로가 없으면 그 큐는 영원히 비고, 빈 큐는 '신고가 없다' 로 읽힌다**. 자기 티켓만 읽는다 | S8-09 |

> **플래너 계정은 전용이다**(S6-01). `planner@local.test` — 소비자 계정을 겸하면 "플래너"와 "커플 구성원"이 같은 사람이 되어 위임 격리 검사가 **엉뚱한 이유로 통과**한다. 시드는 플래너 레코드와 **활성 위임**(`couples`·`carts`·`consultations` — 채팅·결제는 닫힌 채)까지 만들고, **카테고리 선택(`planner_scopes`)은 시드하지 않는다** — 그것은 고객의 행위이고 `rls-check` 가 트랜잭션 안에서 만들었다 되돌린다.

> **왜 커플 계정이 네 개인가**(S4-04). `db:rls` 는 이미 존재하는 커플이 필요하고, S0-02 의 온보딩 데모는 `couple-a`·`couple-b` 에 커플이 **없어야** 한다. 같은 쌍으로 둘 다 만족시킬 수 없다. 플래그로 가르는 방법도 있었지만 기억해야 할 모드가 생기고 기본값에서는 한쪽이 깨진 채로 남는다. 계정을 두 개 더 두면 **플래그 없이 둘 다 항상 동작한다** — `couple-a`·`couple-b` 는 그대로 두고 `couple-linked-*` 가 픽스처를 맡는다.

> 연동 커플에는 **커플·멤버·온보딩 답변만** 시드한다. 장바구니·찜·채팅방은 넣지 않는다 — `rls-check` 가 트랜잭션 안에서 만들고 되돌리는 픽스처와 충돌하고(활성 장바구니 부분 유니크, `uq_chat_rooms_couple_vendor`), 영구 데모 데이터가 있으면 빈 상태 화면을 검토할 수 없다.

> 시드 업체는 `pending` 으로 시작한다(승인 플로우 데모). **채팅·상담 예약은 승인된 업체하고만 열린다**(`is_active_vendor`, S4-01) — `/admin/vendors` 에서 승인한 뒤에 `/explore/[vendorId]` 의 '업체에 문의하기' 가 동작한다.

> **위 표의 두 행(`/onboarding`·`/admin/vendors`)이 인용문 아래로 떨어져 나와 있었다** —
> 표 중간에 인용문이 끼면서 마크다운 표가 끊겼고, 두 행은 표가 아니라 본문으로 렌더됐다.
> **T-00h 가 표 안으로 되돌렸다.** 표 사이에 인용문을 넣지 않는다.

#### 이 표에 없던 것 · 도달 경로가 없는 것 (T-00h)

| 항목 | 내용 |
|---|---|
| **빠져 있던 화면 1** | `/search`(S7-02). 실제 라우트 **59개** 중 이 표에 **58개**만 있었다. 위 표에 넣었다. **S8-01 이 `/admin` 을, S8-02 가 `/admin/audit` 을, S8-04 가 `/admin/privacy` 를, S8-03 이 `/admin/disputes`·`/admin/consultation-disputes` 를, S8-10 이 `/admin/prices` 를 더해 65개가 됐다** |
| **도달 불가 4** (5 → 4) | `/admin/commission-rates` · `/admin/settlements` — 둘 다 `ADMIN_NAV` 에 없다. ~~`/admin/penalties`~~ 는 **S8-03 이 내비에 이었다**(분쟁 조율 바로 아래 — 큐가 위약금 건을 그리로 넘긴다). `/vendor/cancellations` · `/vendor/escrow` — 둘 다 `VENDOR_NAV` 에 없다. **화면은 완성돼 있고 URL 을 직접 쳐야 열린다**(FIX-25) |
| **id 가 필요한 것 3** | `/checkout/[bookingId]` · `/bookings/[id]/cancel` · `/bookings/[id]/escrow`. 진입 화면 `/bookings/[id]`(S5-10)가 없고 **시드가 예약·계약 행을 만들지 않아** 넣을 id 자체가 없다 |
| **죽은 링크 5** (8 → 7 → 6 → 5) | `ADMIN_NAV` 의 `/admin/quality`·`/admin/content`·`/admin/tickets`·`/admin/settings` 와 `VENDOR_NAV` 의 `/vendor/bookings`. ~~`/admin/prices`~~ 는 **S8-10 이 만들어 살아났다**. ~~`/admin/disputes`~~ 는 **S8-03 이 만들어 살아났다**. **누르면 404 다**(FIX-23). ~~`/admin`~~ 은 **S8-01 이 만들어 살아났다** — 내비는 손대지 않았고 가리키던 곳이 생겼을 뿐이다. `BottomTabNav` 5개는 전부 정상 |

API 라우트 — `POST /api/vendor/apply`, `GET/PUT /api/vendor/profile`,
`GET/POST /api/vendor/products`, `PATCH/DELETE /api/vendor/products/[id]`,
`GET/POST /api/vendor/products/[id]/options`, `PATCH/DELETE .../options/[optionId]`,
`GET/POST /api/vendor/members`, `PATCH/DELETE /api/vendor/members/[userId]`,
`POST /api/vendor/inventory/bulk`,
`GET/POST /api/vendor/price-rules`, `PATCH/DELETE .../[id]`, `POST .../simulate`,
`GET /api/vendor/stats`,
`GET/POST /api/onboarding`, `GET/POST /api/couples/invite`, `GET /auth/callback`,
`GET /api/vendors`, `GET /api/vendors/[id]/availability`,
`GET/POST/DELETE /api/cart`, `GET/POST/DELETE /api/wishlist`, `GET /api/prices`,
`PUT/DELETE /api/me`, `POST/DELETE /api/me/delete-request`,
`GET/POST /api/chat/rooms`, `GET/POST /api/chat/messages`, `GET/POST /api/vendor/chat`,
`GET/POST /api/inquiries`, `GET/POST /api/vendor/quotes`,
`GET/POST /api/vendor/settings`, `GET/POST /api/vendor/invites`,
`GET/POST /api/vendor/invites/accept`,
`GET/POST /api/consultations`, `POST /api/consultations/[id]/confirm`,
`GET/PATCH /api/vendor/consultations`, `GET/POST /api/vendor/availability`,
`POST /api/jobs/consultation-confirm-request`, `POST /api/jobs/consultation-resolve`(서비스롤 키 필요),
`GET/POST /api/qna`, `GET/POST /api/vendor/qna`, `POST /api/jobs/sla-escalation`(서비스롤 키 필요),
`GET/PUT /api/notifications`, `POST /api/jobs/dday-notifications`(서비스롤 키 필요),
`POST /api/contracts`, `POST /api/contracts/[id]/sign`,
`GET /api/payments/schedules`, `POST /api/payments/checkout`,
`POST /api/payments/webhook`(**세션이 아니라 서명으로 인증한다**),
`GET/POST /api/bookings/[id]/cancel`, `POST /api/cancellations/[id]/confirm`,
`GET/POST /api/admin/penalties`, `POST /api/escrow/release`(이행 확인·운영자 조율),
`GET /api/vendor/settlements`(`?format=csv` 는 세금계산서 **자료**),
`POST /api/admin/settlements/run`(집계·확정·지급),
`GET/POST/PATCH /api/admin/commission-rates`, `GET /api/admin/commission-rates/resolve`,
**`GET /api/admin/metrics`**(운영자 KPI 집계 · `?days=7|30|90` · `force-dynamic`),
**`GET /api/admin/audit-logs`**(감사 로그 검색 · `?format=csv`), **`GET /api/admin/entity-events`**(대상 타임라인 · 읽기 전용),
**`GET/PATCH /api/admin/privacy-audit`**(개인정보 감사 · 삭제 요청 처리), **`POST /api/jobs/purge-documents`**(파기 배치 · 서비스롤 키),
**`PATCH /api/admin/disputes/[id]`**(조율안·합의 기록), **`GET/PATCH /api/admin/consultation-disputes`**(노쇼 조율 · `applyVerdict` 를 그대로 부른다),
**`POST /api/admin/prices/recalculate`**(지수 재계산·표본 제외), **`GET/PATCH /api/admin/price-anomalies`**(이상 탐지 큐 · 미결이면 `blocked`),
**`POST /api/jobs/price-index-refresh`** · **`POST /api/jobs/price-anomaly-scan`**(배치 · 서비스롤 키),
`PATCH /api/admin/vendors/[id]/review`,
**(T-00h 가 더한 16개 — 7단계·6단계 산출물인데 이 목록이 따라오지 않았다)**
`GET/POST /api/search`(S7-02), `POST /api/ai/planner`(**SSE** · S7-06),
`POST /api/documents` · `POST /api/reports` · `GET /api/reports/[id]`(**202 + 폴링** · S7-03),
`GET/POST/PATCH /api/tasks`(S7-08),
`GET /api/planners` · `GET/PUT/POST /api/planner/profile`(S6-02),
**`GET/POST /api/planner-engagements`** · **`PATCH /api/planner-engagements/[id]`**(S6-04 · **§4.2 에 없던 행이다**),
`GET/PUT /api/planner-scopes`(S6-03 · **계약 발행이 읽는 값이다**),
`GET /api/planner/settlements` · `POST /api/admin/planner-payouts` · **`POST/GET /api/jobs/planner-payout-due`**(S6-05 · 배치는 `CRON_SECRET` 또는 서비스롤 키로 부른다),
`GET/POST /api/community/posts` · `GET/PUT/DELETE /api/community/posts/[id]` ·
`GET/POST /api/community/posts/[id]/comments` · `POST/DELETE .../like` · `POST/DELETE .../scrap` ·
`POST /api/community/reports`(S7-15), `GET/PATCH /api/vendor/community-tags`(S7-16),
`GET/PATCH /api/admin/community-reports`(S7-17).

> **라우트 수 (2026-08-16 실측)** — `app/**/page.tsx` **59** · `app/**/route.ts` **72**.
> 위 목록은 그 72개를 전부 담는다(`GET /auth/callback` 포함). **표를 갱신하지 않고 화면·API 를
> 더하지 않는다**(아래 규칙) — T-00h 이전에 화면 1·API 16 이 목록 밖에 있었다.

> **알림 발송은 `lib/notify/send.ts` 의 `sendNotification()` 으로만 한다**(S4-13). `notifications` 에 직접 insert 하지 않는다 — 수신 설정 확인·멱등·발송·결과 기록이 한 함수 안에 있고, 나눠 쓰면 그중 하나가 빠진다. 로컬 스텁은 **아무 데도 보내지 않고** 기록만 남긴다(Mailpit 을 쓰지 않는 이유는 `lib/notify/stub.ts` 주석 참조).
>
> D-day 배치는 `POST /api/jobs/dday-notifications` 를 서비스롤 키로 부른다.

> **증적 기록은 `lib/audit/record.ts` 의 `recordEvent()` 로만 한다**(S4-03). `entity_events` 에 직접 insert 하지 않는다 — `entityType` 이 테이블 이름으로 좁혀져 있고 RLS 열람 정책이 그 전제 위에 서 있다. 원문·개인식별정보를 넣을 자리는 애초에 없다(§7.3).

> **소비자 하단 탭**은 홈·탐색·장바구니·찜·마이 다섯이다(S3-11·S3-09). 다섯이 상한이라 여기서 더 늘리지 않는다. 비교·업체 상세·참가격·**채팅**은 2차 화면이라 각각의 1차 화면에서 들어간다.

> **장바구니는 커플당 하나가 아니다**(S3-12 / IDEA-01). 활성 최대 N개이며 값은 `app_settings.cart.max_active`(초기 5)가 갖고 **판정은 DB 트리거**다 — 생성이 요청마다 하나씩이라 API 카운트는 배우자의 동시 요청을 지나간다. 설정 행이 없으면 **1로 좁힌다**(0016 의 옛 동작). 순번은 **빈 번호를 채우고**, 치우기는 삭제가 아니라 `abandoned` 다(증적이 가리킬 행을 없애지 않는다). 판정은 전부 `lib/core/cart/multi-cart.ts` 한 곳이다.

> **총액만으로 우열을 정하지 않는다**(S3-12). 장바구니끼리 비교에서 담은 카테고리가 다르면 '가장 낮은 총액' 배지를 달지 않는다 — 덜 담은 쪽이 싼 것은 당연하고 그것을 우승으로 적으면 표가 거짓말을 한다. 예산이 미정이면 기준선을 **아예 그리지 않는다**(0원 대비로 말하지 않는다).

> **업체 알림은 두 층이다**(S4-14). 조직이 대상과 채널을 정하고(`vendor_settings`·`vendor_notification_prefs`) 개인이 그 안에서 자기 채널을 끈다(`notification_prefs`) — **둘 다 통과해야 나간다.** 개인 설정만 두면 새 staff 가 들어올 때마다 아무도 못 받고, 조직 설정만 두면 개인이 야간 푸시를 끌 수 없다. `in_app` 은 어느 층에서도 끌 수 없다. 발송 대상 판정은 `lib/vendor/settings.ts` 의 `vendorDelivery()` 한 곳이며 채팅·문의·상담이 같은 함수를 쓴다.

> **영업시간은 SLA 판정에 쓰지 않는다**(S4-14). 쓰면 **업체가 자기 SLA 기준을 자기가 정하는 구조**가 되고, SLA 는 고객 보호 장치다(F-V-15). 대신 셋으로 가른다 — 판정 기준은 벽시계 그대로 / 알림 발송 시각은 영업시간을 존중(다음 영업 시작으로 미룸) / 고객 안내에 영업시간을 적어 기대를 맞춘다.

> **업체 초대 토큰은 커플 초대 코드와 다르다**(S2-09). 커플은 **말로 불러 주는** 8자 코드라 짧고 헷갈리는 글자를 뺐지만, 업체는 **이메일 링크**라 옮겨 적을 일이 없고 권한이 가격·정산에 닿으므로(§3.9) 긴 랜덤 토큰이다. 수락은 **초대받은 이메일로 로그인해야** 되고, 수락 처리만 서비스롤이다(`vendor_members` INSERT 가 owner 전용이라). **미가입자의 발송 증적은 `vendor_invites.sent_at`** 이 갖는다 — `notifications.user_id` 가 `auth.users` FK 라 계정 없이는 행을 만들 수 없다.

> **보증금은 플랫폼의 벌금이 아니다**(S4-07 · D-22 · D-24). 양측이 합의한 조건을 플랫폼이 보관했다가 **§3.11 규칙대로** 집행하는 것이고, 그래서 종결에는 사유가 반드시 붙는다(`consultation_deposits_resolved_chk`). 판정은 `lib/core/consultation` 의 **순수 함수 하나**가 하고 화면·API·배치가 그것을 부른다 — 규칙이 세 곳에 흩어지면 세 곳이 다른 답을 낸다. **양측 무응답의 기본값은 환불**이다: 몰취가 기본이면 업체는 방치가 이득이 되고 확인 절차가 형해화된다.

> **결제 어댑터는 골격이다**(S4-08 · D-28). `lib/payments` 에 인터페이스 + 로컬 스텁이 있고 실연동은 토스 계약 후다. **프로덕션에서 스텁이 돌면 예외로 끊는다** — 알림 스텁은 안 보내고 보냈다고 하는 것이지만, 결제 스텁은 있지도 않은 돈을 두고 몰취 판정이 돌아가게 만든다. 멱등 경계는 `consultation_deposits.idempotency_key` 유니크이며 스텁도 같은 계약을 지킨다.

> **회차 결제는 DB 가 마지막 경계다**(S5-06 · 0030). 승인(`paid`)이 되려면 셋이 동시에 참이어야 한다 — **계약이 확정(active)** · **결제 전 고지·동의 기록 2종** · **상태와 시각의 짝**. 앱에서만 검사하면 결제 경로가 늘 때(재시도 배치·운영 보정) 한 곳만 빠뜨려도 고지 없는 결제가 장부에 남는다. 회차당 **진행 중 1건·성공 1건**이 부분 유니크이고, 종결 상태는 되돌릴 수 없다(D-23). 회차는 결제 실패로 되돌리지 않는다 — `void` 로 만들면 비율 합이 깨진다.

> **결제 멱등은 두 방향이다**(S5-06). 나가는 요청은 **서버가 회차 id 로 만든다**(`paymentIdempotencyKey`) — 클라이언트가 정하면 매 요청이 새 열쇠가 되어 같은 회차가 두 번 결제된다. 들어오는 웹훅은 `(provider, event_id)` 유니크가 판정하며, 순서는 **서명 검증 → 중복 확인 → 상태 전이**다. 서명이 틀린 요청은 **수신 원장에 행을 만들지 않는다** — 만들면 남이 보낸 이벤트 id 로 진짜 이벤트를 막을 수 있다(선점). 서명 비밀이 없으면 **거부한다**(닫힌 쪽으로 실패).

> **결제 실패는 화면 전환이 아니다**(S5-06). 같은 화면의 결과 영역에 **다음 행동과 함께** 적는다. 재시도 상한(`app_settings.payment.max_attempts`)에 닿으면 다른 결제 수단이나 고객센터를 안내한다. API 는 실패를 **402** 로 돌려준다 — 200 으로 내리면 화면이 성공으로 그린다.

> **정산은 결제와 함께 서지 않는다**(S5-06 · O-15). `settlement.fee_basis` 가 미결이면 정산을 세울 수 없지만 **결제를 막지 않는다** — 고객이 낼 금액은 계약 총액이 정하고, 수수료를 할인 전·후 어디서 뗄지는 플랫폼과 업체 사이의 문제다. 대신 보류를 `entity_events`(`settlement_deferred`)로 남겨 **셀 수 있게** 한다. 플래너 수수료(D-17)는 **계약 확정 시점**에 발생한다 — 결제가 아니다. 계약은 서명으로 성사되고 결제는 그 이행이다.

> **위약금 엔진을 두 벌 만들지 않는다**(S5-08). 구간 판정·금액 산정은 T-04 의 `lib/core/pricing/penalty.ts` 하나이고, 해지 태스크는 그 **결과를 환불·잔여 청구로 나누는 일**만 한다. 룰 수치는 `lib/pricing/penalty-rule-set.ts` 가 주입하며 **DB(`penalty_rules`) 우선·코드 가정치 폴백**이다. **가정치 시드를 넣지 않았다** — 법무 검수 전 수치를 DB 에 넣으면 그것이 운영 기준처럼 굳는다(0029 가 조항 문안에서 내린 판단과 같다). 확정되면 **행을 넣기만 하면** 코드 변경 없이 전환되고, 그때까지 `isDraft` 가 화면까지 실려 나간다(§7.7).
>
> **해지에서 귀책은 주장과 확정이 다르다**(S5-08 · D-24). 당사자가 고른 값은 `couple_claim`·`vendor_claim` 이고 `fault` 는 **양측 일치 또는 운영자 조율**로만 정해진다. 나누지 않으면 사유 선택 하나가 곧 정산 결과가 된다. **귀책 미정으로는 정산할 수 없고** DB 트리거가 그것을 막는다. 업체 귀책이면 위약금 없이 전액 환불이지만 **배상액은 코드가 만들지 않는다**(O-03 대기 — 조율 대상).
>
> **무응답을 동의로 읽지 않는다**(S5-08). S4-07 은 상담 보증금에서 "양측 무응답의 기본값은 환불" 로 정했지만, 계약 해지는 **금액이 크고 귀책에 따라 결과가 정반대**라 기본값을 만들 수 없다. 기한이 지나면 `/admin/penalties` 조율 큐로 간다 — 자동 집행보다 사람이 보는 편이 낫다.
>
> **예약 확정이 재고 자리를 잡는다**(S5-08 · S2-05 가 남긴 자리). 계약 확정(`contracts.active`, 서명의 사건)과 예약 확정(`bookings.confirmed`, 자리의 사건)은 **다른 사건**이다. `inventory_slots.remaining` 은 후자에서 줄고 취소에서 되돌아오며 트리거가 판정한다(동시 확정은 `for update` 잠금이 막는다). **자리 없는 계약도 가능**하고(스튜디오·드레스), 자리가 붙어 있는데 남은 자리가 없으면 확정을 막는다. **확정된 예약은 자리를 옮길 수 없다** — 취소 후 다시 예약해야 두 단계가 각각 판정된다.
>
> **환불은 나중 회차부터**(S5-08). 계약금(1회차)이 위약금의 기준이자 마지막까지 남는 돈이고(T-04 `refundDeposit`·검출 룰 R-02), 잔금부터 돌려주면 부분 환불이 회차 경계를 덜 넘는다. **미납 회차를 `void` 로 만들지 않는다** — 0028 의 비율 합 트리거가 커밋을 막고, 계약이 `cancelled` 가 되면 0030 이 이미 결제를 막는다.
>
> **이미 지급된 플래너 수수료를 코드가 회수하지 않는다**(S5-08). `earned`·`payable` 은 `void` 로 무효화하지만 `paid` 는 **회수 대상으로 표시하고 조율로 넘긴다** — 계좌로 나간 돈을 자동으로 되가져올 방법이 없고 상계할 다음 지급이 있을지도 코드가 알 수 없다.
>
> **미결정으로 막힌 정산은 '실패' 가 아니라 '설정 대기' 다**(S5-07 · O-15). `settlement.fee_basis` 가 비면 정산서를 `blocked` + `blocked_reason` 으로 남기고 화면은 **설정 대기**로 적는다 — '실패' 로 그리면 업체는 장애로 이해해 문의하고 운영은 없는 장애를 코드에서 찾는다. **거래 내역은 이미 모여 있고** 기준만 채우면 **같은 행이 재계산**된다(새 행을 만들지 않는다 — 증적이 가리키던 id 가 바뀐다). 기준은 계산이 선 시점에 **스냅샷으로 박힌다**(D-16 과 같은 이유).
>
> **확정된 정산서는 고치지 않고 상계로 넘긴다**(S5-07 · D-23). 동결 트리거가 금액 변경을 막고, 조정은 `settlement_adjustments` 에 쌓여 **다음 정산이 가져간다**. 상계는 **건 단위로 들어가거나 통째로 이월**되며(쪼개면 한 건이 두 정산서에 걸려 재현이 어렵다) **지급액은 음수가 되지 않는다**. `settlement_items.adjustment`(0003)와 다른 층이다 — 그쪽은 건별, 이쪽은 **기간을 건너뛰는** 조정이다.
>
> **정산 지급도 어댑터 + 스텁이다**(S5-07 · D-28). 결제 스텁과 같은 이유이고 방향만 반대다 — 결제 스텁이 돌면 받지 않은 돈을 받았다고 적히고, **지급 스텁이 돌면 보내지 않은 돈을 보냈다고 적힌다.** 업체는 '지급 완료' 를 보고 오지 않는 입금을 기다리며, **우리 장부는 정상으로 보인다.** 프로덕션 기본값은 `noop`(실패를 돌려준다)이고 `PAYOUT_ADAPTER=stub` 은 예외로 끊는다. 멱등 열쇠는 `settlement:<id>:payout:<attempt>` 이며 **자동 재시도에서 회차를 올리지 않는다**.
>
> **정산 금액은 업체가 쓸 수 없다**(S5-07 · §3.9). 열람은 **대표 전용**(0028 이 staff 를 DB 에서 막았다)이고, 쓰기는 **이의 제기 메모(`vendor_note`) 하나뿐**이다 — 그것도 정책이 아니라 **컬럼 권한**으로 좁혔다. 정책은 행을 가르고 컬럼을 가르지 않으므로 이의 제기 정책만 두면 금액까지 열린다.
>
> **세금계산서는 자료까지다**(S5-07 · F-V-09). 발행에는 국세청 연동 또는 대행사 계약(D-28)과 **사업자 정보 평문**이 필요한데 후자를 우리가 갖고 있지 않다(§7.2 — `biz_no_enc` 는 해시). 그 상태에서 발행을 흉내 내면 **세금계산서처럼 보이는 문서**가 생기고 회계에서 실제로 쓰인다. 과세 대상은 **정산액이 아니라 중개 수수료**다 — 거래 대금은 고객과 업체 사이의 것이고 플랫폼이 제공한 용역의 대가는 수수료뿐이다(D-24).
>
> **리뷰 대가 쿠폰은 스키마가 막는다**(S5-11 · §7.7 · D-03). `coupons.issue_condition` 을 자유 텍스트로 두면 "후기 작성 시 5천원" 이 언제든 들어온다 — 허용 값 집합을 CHECK 로 제한하고 리뷰·후기·평점 관련 값을 **집합에 넣지 않는다.** `db:rls` 가 코드 목록과 DB CHECK **양쪽**을 본다. 정률 쿠폰에는 **상한이 필수**이고(상한 없는 정률은 업체 정산을 통째로 지운다) 만료일은 **발급 시점 스냅샷**이다(D-16). 부담 주체(`borne_by`)도 사용 시점에 박아 과거 정산이 소급 변경되지 않게 한다.
>
> **정책끼리 서로를 조회하면 무한 재귀다**(S5-11). `coupons` 정책이 `coupon_issues` 를 보고 그쪽이 다시 `coupons` 를 보면 Postgres 가 그 자리에서 멈춘다(`infinite recursion detected in policy`). 0016·0028 이 쓴 방법대로 **조인을 `security definer` 함수로 감싸** 고리를 끊는다 — 새 표에 교차 참조 정책을 쓸 때 먼저 이 함정을 확인한다.
>
> **요율이 하나도 없으면 거래가 서지 않는다**(S5-03 · F-A-15). `commission_rates` 가 0행이면 계약 발행이 `CONTRACT_RATE_UNRESOLVED` 로 막히고 결제·정산·해지가 전부 따라 막힌다. 명세 §부록이 F-A-15 를 **"개발 블로커 해제 장치"** 라고 적은 이유이며, **로컬 데모 요율은 `seed-accounts.mjs` 가 만든다** — 값이 미확정(O-02)이라 마이그레이션에 넣지 않았다(0031 이 위약금 가정치에서 내린 판단과 같다). 운영 값은 `/admin/commission-rates` 에서 넣는다.
>
> **요율 이력은 지우지 않는다**(S5-03 · D-23). 과거 계약의 요율은 스냅샷으로 박혀 있지만(D-16) **그 스냅샷이 어디서 나왔는지**를 답하는 것은 요율 표다. 그래서 변경은 **새 행**, 종료는 **`effective_to` 를 닫는 것**이고, 0034 가 두 표의 **INSERT·UPDATE·DELETE 권한을 회수**했다 — API 에도 DELETE 라우트가 없다. 쓰기는 서비스롤 경유이고 **열람만 정책으로** 연다(§5.5).
>
> **요율 해석은 한 곳이다**(S5-02). 관리 화면의 시뮬레이터도 `resolveRate` 를 그대로 부른다 — 화면용 계산을 따로 만들면 언젠가 시뮬레이터와 실제 계약이 다른 요율을 말한다. 겹침은 0006 의 EXCLUDE 가 최종 경계지만 **저장 전에 어느 행과 부딪혔는지**를 먼저 알려준다.
>
> **에스크로 예치 대상은 잔금이다**(S5-09 · F-C-16). 계약금(1회차)은 계약 성립의 증표이자 위약금의 기준이라(T-04 · 검출 룰 R-02) 묶지 않는다 — 묶으면 **계약이 성립해도 업체는 아무것도 받지 못한 채** 예식일까지 준비 비용을 먼저 쓴다. 회차가 늘어도 규칙은 "첫 회차를 뺀 나머지" 다.
>
> **에스크로의 무응답 폴백은 S4-07 과 반대다**(S5-09). 보증금은 "몰취가 기본이면 업체의 방치가 이득" 이라 환불을 기본으로 뒀지만, 에스크로는 **환불이 기본이면 고객의 방치가 이득**이 된다 — 서비스는 이미 이행됐는데 확인만 안 하면 업체가 영원히 돈을 못 받는다. 그래서 **기본값은 릴리즈**이고, 다만 **예식일 경과를 조건으로** 넣는다(예식 전에는 이행이 없었다). 한쪽이라도 "이행되지 않았다" 고 하면 **즉시 조율**이라 자동 릴리즈가 그것을 덮지 않는다.
>
> **양측 확인 판정은 공통이고 해석은 도메인이 한다**(S5-09). `lib/core/confirmation/two-sided.ts` 가 `대기/일치/불일치/기한경과` 까지 판정하고, **기한 경과의 뜻**은 호출부가 정한다 — S4-07 환불 · S5-08 조율 · S5-09 릴리즈. S4-07 은 불리언이 아니라 4값 outcome 대조라 **옮기지 않았다.**
>
> **에스크로 실예치는 두 층을 지나야 한다**(S5-09 · D-28 · O-03). `app_settings.escrow.enabled`(전자금융업 등록 등 **O-03 대기라 false**)와 어댑터(프로덕션에서 스텁 거부). 둘 다 통과해야 돈이 움직이고 그 전까지는 절차·기록이 돈다. **어댑터는 PG 사 위탁을 수용하는 모양**이다 — 자금 보관은 요건이 무거워 직접 보관보다 위탁이 현실적일 수 있고, 인터페이스는 "우리가 계좌에 넣어 둔다" 를 전제하지 않는다.
>
> **보관 중인 돈은 정산에 들어가지 않는다**(S5-09 → S5-07). 완납과 이행 확인은 다른 사건이다 — 잔금을 냈다는 것과 서비스가 이행됐다는 것은 별개다. 열린 홀드가 있으면 그 예약은 **이번 기간에서 빠지고 다음 기간에 다시 후보**가 된다.
>
> **플래너는 축이 둘이다**(S6-01 · D-43 초안). `planner_engagements`(0004)는 **열람 권한 위임**(표 단위 · RLS 의 `has_planner_scope` 가 판정)이고 `planner_scopes`(0036)는 **카테고리별 이용 여부**(과금)다. 합치면 "예산은 보게 하되 어느 카테고리에도 플래너를 쓰지 않는다"(상담만 받는 단계)를 표현할 수 없다. **한쪽을 끄면 다른 쪽도 꺼진다고 가정하지 않는다** — 카테고리를 빼도 위임은 살아 있으므로 화면이 두 경로를 함께 안내한다.
>
> **플래너 카테고리 선택은 삭제하지 않는다**(S6-01 · D-23). "언제부터 언제까지 이 카테고리에 플래너를 썼는가" 가 정산 분쟁의 질문이라 해제는 `status='released'` + `released_at` 이고 **재선택은 새 행**이다(동시 선택 1건은 부분 유니크). **요율은 여기 없다** — `planner_fee_rates` 가 갖고 계약 확정 시 스냅샷된다(D-16). **활성 위임이 있는 플래너만** 카테고리에 지정할 수 있다(트리거) — 없으면 보지도 못하는 플래너에게 수수료가 붙는다.
>
> **카테고리 선택은 커플 구성원 누구나 한다**(S6-01). 결제·계약 서명이 owner 전용인 것과 다른 층 — 이것은 **구성 선택**이지 돈을 움직이는 확정이 아니고(과금은 계약 확정 시 스냅샷) 장바구니 항목 토글을 배우자도 바꿀 수 있는 것과 같다. **플래너는 읽기만** 한다 — 스스로 범위를 넓히면 자기 수수료를 늘리는 행위다.
>
> **검출 룰은 코드가 갖고 DB 는 스위치를 갖는다**(S7-01). 정규식은 `lib/core/rules/detect-rules.ts` 가, 켬/끔(`is_active`)·지시문·근거·판본은 `detect_rules` 표가 갖는다. **DB 정규식을 컴파일하지 않는다** — 운영자가 한 글자 잘못 적으면 스캔이 통째로 멈추거나(SyntaxError) 특정 문서에서 되돌아오지 않는다(파국적 백트래킹). 검출이 코드로 도는 탓에 **사본이 벌어져도 화면에는 아무 일도 안 생기므로** `npm run db:rls` 가 코드↔DB 를 대조한다. 룰을 고칠 때는 **코드를 먼저 고치고 `seed.sql` 블록을 다시 만든다.**

> **위약금 기준 표(`penalty_rules`)는 일부러 비어 있다**(S5-08 · S7-01). 법무 검수 전 가정치를 DB 에 넣으면 그것이 운영 기준처럼 굳는다. 로더(`lib/pricing/penalty-rule-set.ts`)가 **DB 우선·코드 폴백**이라 확정되면 **행을 넣기만 하면** 코드 변경 없이 전환된다. `db:reset` 뒤 이 표가 0행인 것은 정상이며 `db:rls` 가 그 결정을 검사로 붙잡아 둔다.

> **플래너의 공개는 심사의 결과다**(S6-02). `planners.status` 는 `pending` 으로 시작하고 **본인은 `active`·`rejected` 로 바꿀 수 없다**(트리거 · 서비스 롤은 통과). 내리는 것(`paused`)만 본인이 한다. **승인을 누를 운영자 화면이 아직 없다**(FIX-17) — 지금 마켓에 보이는 플래너는 시드가 서비스 롤로 넣은 행이다.

> **플래너 요금은 프로필에 없다**(S6-02 · D-16). `fee_json = '{}'` 을 CHECK 가 강제한다 — 프로필에 숫자를 적으면 화면과 실제 청구가 어긋나고 **스냅샷은 소급되지 않으므로** 그 차이를 나중에 맞출 방법이 없다.

> **실적 집계는 뷰가 아니라 함수다**(S6-02). 계약 건수의 출처인 `planner_settlements` 는 소유자 전용이라 뷰로 열면 **남의 정산 금액이 새어 나간다**(FIX-13·FIX-14 계열). `planner_contract_count(uuid)` 를 SECURITY DEFINER 로 두어 **개수만** 돌려주고 anon 에도 부여했다 — 마켓은 비로그인이 보는 화면이다. **뷰를 만들 때는 소유자 필터를, 못 넣겠으면 뷰를 만들지 않는다.**

> **플래너 랭킹은 지금 지표 하나만 셀 수 있다**(S6-01 · D-25 · O-13). 계약 건수(`planner_settlements`)뿐이고 상담(S6-04)·예약(S6-03)·리뷰(S8-11 — `reviews` 는 업체 대상이라 플래너 후기 구조가 없다)·프로필 방문(S8-10)은 **0이 아니라 "아직 세지 않는다"** 로 표시한다. **가중치를 지어내지 않았다** — 종합 점수를 만들면 플래너의 수입이 우리가 지어낸 계수에 좌우된다. **신규 플래너 콜드스타트**(실적 0이 영원히 하위 → 진입 장벽)도 O-13 에 함께 적어야 한다.
>
> **상담 슬롯 중복은 EXCLUDE 로 막는다**(0025). 예약은 시각 하나가 아니라 **구간**을 차지하므로 UNIQUE 로는 14:00·14:30 겹침을 못 잡는다. 자리를 차지하는 상태는 `approved`·`confirmed` 뿐이고 `requested` 는 아니다 — 업체가 후보를 여럿 받아 고르는 흐름을 막지 않기 위해서다. 판정이 **승인 시점에 DB 에서** 일어나므로 동시 승인에서도 하나만 통과한다.

> **견적은 플랫폼 폼으로만 오간다**(S4-12). 견적 항목은 `products`·`product_options` **참조로만** 존재하고(CHECK), 이름·분류·옵션 상한은 **트리거가 등록된 행에서 덮어쓴다** — 항목 자리에 자유 텍스트를 넣을 방법이 없다. 금액은 `price_rules` 평가 결과를 상한으로 삼아 **할인만** 되고 할증은 CHECK 가 막는다. 상한 계산이 `lib/core/pricing` 의 순수 함수라 DB 가 스스로 검증할 수 없어 **`quotes`·`quote_items` 쓰기 권한을 회수하고 서버(서비스롤) 경유로 좁혔다**. 견적에 **첨부 컬럼을 두지 않는다** — 파일로 보내는 순간 플랫폼 밖 양식이 되고 위 장치가 전부 무의미해진다.

> **업체에 말을 거는 길이 셋이고 결과물이 다르다** — 문의·견적(`/inquiries`)은 견적서를, 채팅(`/chat`)은 합의를, 문의게시판(`/qna/[vendorId]`)은 공개 지식을 남긴다. 문구는 `lib/core/inquiry/inquiry.ts` 의 `CONTACT_PATHS` 가 갖고 `ContactPathGuide` 가 문의함·업체 상세에 같은 문장을 보여준다.

> **실시간은 Supabase Realtime 이다**(S4-04 · O-11 · D-29 초안). publication 에 들어 있는 표는 **`chat_rooms` 하나뿐**이다 — `chat_messages` 를 구독하면 본문이 소켓을 타고 회수 가림막(`chat_messages_visible`)을 우회한다. 규칙은 **소켓은 신호, 진실은 재조회**이며 구독 훅은 `hooks/use-room-signal.ts` 다. 새 표를 실시간으로 열 때 같은 판단(본문이 payload 에 실리는가)을 먼저 한다.

> **채팅 첨부는 서버를 지나가지 않는다.** 서버는 서명 업로드 URL 만 발급하고 클라이언트가 Storage 로 직접 올린다(§5.3 — 원문이 로그·트레이스에 실릴 자리를 만들지 않는다). 내려받기 주소는 볼 때마다 새로 발급하며 **DB 에 저장하지 않는다**(만료되고, 저장하면 접근권이 샌다).

> **AI 플래너는 상한 파라미터가 있어야 열린다**(S7-20 · D-49). `ai.free_daily_turns`·`ai.session_token_cap` 이 비어 있으면 대화를 시작하지 않는다 — 없는 상한을 '무제한' 으로 읽으면 그 순간 비용 상한이 사라지기 때문이다. `seed.sql` 은 **키만** 만들고(값 null) 로컬 데모 값은 `npm run seed:accounts` 가 넣는다(요율과 같은 방식 · S5-03). 그래서 `db:reset` 뒤 `seed:accounts` 를 건너뛰면 플래너 화면(S7-06)이 "한도가 설정되지 않았다" 로 열린다 — 고장이 아니라 결정이다. **배포 환경에는 그 장치가 없다**(FIX-21).
>
> **견적 비교가 섰다**(S7-05). 원천은 **`quotes`·`quote_items`**(S4-12)이고
> **파싱 단계가 없다**(D-91) — 업체는 표준 폼으로만 응답하며 항목의 이름·분류는
> 트리거가 덮어쓴다(0024). `estimate_uploads`·`estimate_items` 는 **쓰지 않는다**.
> 업체 이름·카테고리는 `estimate_quote_sources()`(SECURITY DEFINER · 0047)가 내린다 —
> 임베드로 읽으면 행이 안 보일 때 견적이 통째로 `unmapped` 가 된다(S7-07 계열).
> **카테고리 표는 예산(S7-07)과 하나**이며 갈리는 곳은 모르는 업종뿐이다(예산 `etc` ·
> 견적 `unmapped`). 비교표는 **누를 때만 스냅샷으로 남고** 그 행이 S7-12 의
> `estimate_comparison` 공유 대기를 풀었다. 확인 경로는 `npm run db:rls` 의 견적
> 구역(18건)과 `tmp/s705-flow.mjs` 다.
>
> **공유 링크가 섰다**(S7-12). `/reports/[id]` 가 "공유는 S7-12" 로 비워 둔 자리이며
> 만료·거둠·뷰 전용·마스킹이 붙었다. **권한은 자원으로 판정한다**(D-88) — `share_links` 에
> `couple_id` 를 두지 않고 "그 자원을 읽을 수 있으면 만들고 거둘 수 있다" 로 정했다.
> 여는 일은 `share_link_open()`(SECURITY DEFINER · 0046)이 하고, **캐시를 꺼야 한다**
> (쿠키를 읽지 않는 경로라 Next 가 굳힌다 · FIX-22 계열). 기한은
> `app_settings.share.link_ttl_hours`(168시간)이며 **비면 발급하지 않는다**(D-90).
> 확인 경로는 `npm run db:rls` 의 공유 구역(16건)과 `tmp/s712-flow.mjs` 다.
>
> **위약금은 엔진·해지 견적·대화에 이어 화면까지 섰다**(T-04 · S5-08 · S7-20 · **S7-04**).
> 네 자리가 **같은 함수**(`calculatePenalty`)를 부른다 — 두 벌이면 같은 계약이 화면과
> 대화에서 다른 금액으로 나온다. **`penalty_rules` 는 여전히 비어 있고**(0031 근거 6 —
> 법무 검수 전 수치가 운영 기준처럼 굳지 않게) 화면이 그 사실을 적는다. **행이 들어오면
> 코드 변경 없이 전환된다**(`loadPenaltyRuleSet`). 확인 경로는 `npm run db:rls` 의
> 위약금 구역(13건)과 `tmp/s704-flow.mjs` 다.
>
> **예산은 화면·API·홈 게이지까지 섰다**(S7-07). 총예산의 진실은 `couples.total_budget`
> 하나이며(D-82) 장바구니 기준선(D-77)과 **같은 칸**이다 — 두 화면이 다른 숫자를 말하지
> 않게 하려는 것이다. **권장은 총예산을 쪼개지 않는다**(D-81): 참가격 지수가 있는
> 카테고리의 중앙값 그 자체이며 **기준이 없으면 권장하지 않는다**(빈칸이지 0원이 아니다).
> 계약·결제는 **저장하지 않고 조회 시점에 세고**(D-84) 분류는 `budget_contracted`
> (SECURITY DEFINER)가 한다 — 임베드로 읽던 초안이 커플이 `vendors` 를 못 읽자 계약을
> 통째로 '기타' 로 떨어뜨렸다. 확인 경로는 `npm run db:rls` 의 예산 구역(26건)과
> `tmp/s707-flow.mjs` 다.
>
> **일정은 표현까지 다 섰다**(S7-18 · S7-08 · **S7-19**). `task_dependencies` 와 순환 방지 트리거(0042) 위에 `/checklist` 와 `GET/POST/PATCH /api/tasks` 가 올라갔고, **자동 생성이 템플릿 순서를 함께 옮긴다**. 역산 템플릿 19종은 `lib/core/schedule/templates.ts` 가 갖고 `seed.sql` 이 사본이며 `db:rls` 가 대조한다. **`ready`·`waiting` 은 컬럼이 아니라 계산값**이고 `GET /api/tasks` 응답에 실린다 — **표현 넷도 같은 응답을 쓴다**(S7-19 · `GET /api/tasks/graph` 가 같은 `loadChecklist` 를 부른다). 선행 편집(`POST/DELETE /api/tasks/[id]/dependencies`)의 **판정은 DB 트리거**이며 거절 사유는 `hint` 로 나간다(0044). 표현 넷의 스위치는 `feature_flags.schedule.views` 이고 **지금은 넷 다 켜져 있다** — 끄는 판정은 **O-16**(S8-01 지표 뒤)이다. 확인 경로는 `npm run db:rls` 의 태스크 구역(37건)과 `tmp/s708-flow.mjs`·`tmp/s718-flow.mjs`·`tmp/s719-flow.mjs` 다.
>
> **커뮤니티가 열렸다**(S7-14 · S7-15 · S7-16 · S7-17). `feature_flags.community.enabled` 가 **true** 이며 0039 가 껐던 스위치를 **0041 이 켰다** — 세 층(본문 필터 · 신고·모더레이션 · '미검증 경험담' 라벨링)과 양측 절차(소비자 신고 · 업체 답변·신고 · 운영자 큐)가 모두 갖춰졌기 때문이다. **집계 값은 열 단위 GRANT 로 잠겨 있다** — `like_count`·`view_count`·`is_pinned` 는 로그인 사용자가 직접 못 고치며, 좋아요는 트리거가·조회수는 `bump_post_view()` 가 올린다. 확인 경로는 `npm run db:rls` 커뮤니티 구역(49건)과 `tmp/s715-flow.mjs`·`tmp/s716-flow.mjs`·`tmp/s717-flow.mjs` 다. **서비스 공개 시점과는 다른 층이다** — 색인 차단은 `ALLOW_INDEXING` 이 따로 쥐고 있다(S3-10).

> **계약서 검토도 키 없이 돈다**(S7-03). 키가 없으면 5단계(LLM)를 건너뛰고 **룰 검출 결과만으로** 리포트를 만든다 — 협상 문구는 지어내지 않고 비운 채 그렇다고 적는다. 로컬 검증은 이 경로로 한다(`tmp/s703-flow.mjs`). **`.txt` 만 읽는다**: `EXTRACT_ADAPTER` 는 `plain`(기본)·`stub` 이며 **프로덕션에서 stub 은 거부된다** — 아무것도 읽지 못한 채 만든 리포트는 '위험 없음' 처럼 보인다.
>
> **`ANTHROPIC_API_KEY` 가 없어도 `/planner` 는 열린다**(S7-06 · D-52). 키가 없으면 `rules_only` 모드가 되어 **조건 검색의 룰 파서**가 문장을 읽고 `search_vendors` 를 부른다 — 대화형 답변만 없고 조회는 된다. 그 모드에서 나가는 문장은 전부 코드가 가진 고정 문구라 지어낸 수치가 생길 자리가 없다. 화면 상단에 그 사실을 적어 둔다. **로컬 검증은 이 경로로 한다**(`tmp/s706-flow.mjs`).
>
> **AI 툴은 커플 데이터를 서비스롤로 읽지 않는다**(S7-20 · D-48). 툴 핸들러(`lib/ai/tools/handlers.ts`)는 세션 클라이언트만 쥐고, 요율·운영 파라미터 같은 참조 데이터만 `lib/ai/tools/reference.ts` 가 서비스롤로 읽는다. 이 경계는 `npm run test`(`boundary.test.ts`)와 `npm run db:rls` 양쪽이 지킨다 — 앞은 소스를, 뒤는 DB 를 본다.
>
> **규칙** 화면·API 라우트를 추가하는 태스크는 **완료 시 이 표를 갱신한다.**
> 표에 없으면 확인할 방법을 모르는 화면이고, 그러면 만들어 두고도 아무도 안 본다.
> 기능 ↔ 태스크 대응은 `docs/TASKS.md` 커버리지 검증표가 별도로 관리한다.

## 6. 환경변수 (.env.local — 커밋 금지)
| 변수 | 범위 | 비고 |
|---|---|---|
| NEXT_PUBLIC_SUPABASE_URL | 클라이언트 허용 | 로컬 http://127.0.0.1:54321 |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | 클라이언트 허용 | **신형 키 형식 `sb_publishable_...`** (구형 JWT `eyJ...` anon key도 함께 출력되지만 신형을 쓴다) |
| SUPABASE_SERVICE_ROLE_KEY | **서버 전용** | **신형 키 형식 `sb_secret_...`** |
| ANTHROPIC_API_KEY / AI_MODEL | **서버 전용** | 클라이언트 번들 유입 금지 |
| TOSS_CLIENT_KEY / TOSS_SECRET_KEY | 클라이언트/서버 | **5단계 거래** 착수 시 발급(07 §1.3) |
| NEXT_PUBLIC_APP_URL | 공통 | 로컬 http://localhost:3000 |
| EXTRACT_ADAPTER | **서버 전용** | `plain`(기본, `.txt` 실제 추출) / `stub`. **프로덕션에서 stub 은 거부된다**(S7-03) — 못 읽은 채 만든 리포트는 '위험 없음' 처럼 보인다. PDF·OCR 추출기는 새 의존성이라 아직 없다 |
| NOTIFY_ADAPTER | **서버 전용** | `stub`(기본, 로컬) / `noop`. **프로덕션에서 stub 은 거부된다**(S4-13) — 보내지도 않고 보냈다고 기록하면 거짓 증적이다 |
| ALLOW_INDEXING | **서버 전용** | 검색엔진 색인 허용 여부. **공개 전에는 비워 둔다** — robots.txt 와 전 페이지 메타가 이 값 하나를 본다(S3-10) |

환경 3단: local(도커) → staging(Supabase 프로젝트 A + Vercel Preview) → production(프로젝트 B + Vercel Production). 스테이징·프로덕션 키는 Vercel 환경변수로만 관리.

## 7. 코드 배치 규칙 (상세는 05·CLAUDE.md)
- 도메인 로직(검출 룰·위약금 엔진·가격 계산·zod 스키마)은 `lib/core/` — React/Next import 금지.
- Claude API 호출은 `app/api/**` 서버에서만, 클라이언트 직접 호출 금지.
- DB 스키마는 `supabase/migrations/` 단일 진실. 대시보드 수동 변경 금지.
- 개인정보: 계약서 원문은 분석 후 즉시 파기, 마스킹 후 AI 전달, 로그에 원문·경로 금지. 테스트 샘플은 `_local_reports\samples\`(git 제외).

## 8. 일상 개발 사이클
1. `dev`에서 기능 브랜치 생성 → 작업. 브랜치명 규약: **`feat|fix|chore|docs/<태스크ID>-<요약>`**
   - 예: `feat/T-05-vendor-onboarding`, `chore/T-02c-asset-convention`, `docs/T-00-spec-v2`
2. DB 변경 시: 마이그레이션 SQL 작성 → `npm run db:reset` 으로 검증 → 커밋 → `npm run db:types`.
3. 커밋 전: **`npm run verify`**(lint · typecheck · test · 시크릿 · 커버리지 요약 · 문서 표 · 자산).
4. PR → dev 병합 → main 배포. **main 직접 push는 GitHub ruleset(main-protection)으로 차단됨.**
5. 새 의사결정 발생 시 `docs/DECISIONS.md` 에 **D-번호를 부여**하고(번호를 주는 자리는 그
   파일 하나다) 04_의사결정로그.md(리포 외부)에는 그 번호를 옮겨 적는다.

### 검증 순서 — 이 순서를 지킨다 (T-00m)

```bat
npm run verify

npm run db:start
npm run db:reset
npm run db:types
npm run seed:accounts
npm run db:rls

:: 빌드는 따로 돈다. 돌리기 전에 dev 서버를 정리한다.
npm run build
```

**`audit` 계열과 `check:escrow`·`check:settlement` 를 돌렸으면 DB 를 되돌린다.**
그 셋은 쓰기를 남기고, 건너뛰고 `db:rls` 를 돌리면 깨진다 — 실제로 겪었다.

```bat
npm run db:reset
npm run seed:accounts
```

> **검증 명령에 파이프를 걸지 않는다.** 파이프가 앞 명령의 종료 코드를 가린다.
> 걸어야 하면 `set -o pipefail` 을 **먼저** 켠다(bash 를 쓸 때).
>
> **`npm run dev` 로 실동작 점검을 하지 않는다.** Windows 에서 `next dev` 가 요청 폭주에
> 전 라우트를 500 으로 만든다(FIX-58). 프로덕션 빌드로 돈다.

## 9. 리포에 포함되지 않는 문서
공개 리포이므로 사업 전략 문서는 제외한다(.gitignore 처리). 아래는 클로드 프로젝트 지식에만 존재한다.
- 00_프로젝트_지침.md / 01_사업개요_컨텍스트.md / 02_시장데이터.md / 04_의사결정로그.md

리포에 포함되는 문서: 03(기술개발 컨텍스트), 05(앱개발·로컬환경 가이드), 06(본 문서), 07(개발명세서),
TASKS.md(태스크), **DECISIONS.md(의사결정 D-26~ )**, ASSETS.md(자산 규약), DESIGN.md(디자인 시스템),
INVENTORY_CSV.md(재고 CSV 형식)

> **`docs/DECISIONS.md` 는 04 의 대체물이 아니다**(T-00i). 04 는 여전히 리포 밖에 있고,
> 이 파일은 **구현하면서 내려진 결정(D-26~)** 을 04 에 옮겨 적을 수 있게 모아 둔 것이다.
> D-01~D-25 는 여기 없다. **새 결정의 번호를 부여하는 자리는 이 파일 하나**로 한다 —
> `TASKS.md` 노트와 `07` 개정 이력이 각각 번호를 붙이다가 **D-29 가 두 결정에 겹쳤다.**

> **NOTE** `docs/DESIGN.md` 는 T-04b(PR #4)에서 생성돼 `dev` 에 병합됐다. 05·06·07이 이 문서를 참조한다.

## 10. Claude(AI)와의 협업 방식
- **클로드 프로젝트(웹)**: 기획·문서·전략 작업, Claude Code용 프롬프트 작성.
- **Claude Code(로컬)**: 코드 작성·리팩토링. 리포의 CLAUDE.md + docs/ 사본을 읽고 작업.
- 개발 명령·코드 제시 시 이 문서의 환경(Windows CMD, npm 단일, npx supabase, 표준 명령)을 준수한다.

## 11. 현재 상태 (2026-09-06 · **T-00m 현행화 — FIX 필수 여섯 해소 시점**)

> **이 절이 오래 낡아 있었다.** 직전까지 T-01~T-00(1단계 착수 전) 상태를 적고 있었는데
> 그 사이 **7단계까지 왔다.** 태스크 단위 진행은 `docs/TASKS.md` 가 진실이며 여기서는
> **환경 관점의 요약**만 둔다 — 두 벌로 적으면 한쪽만 고쳐지는 날이 온다.

### 한 줄 요약

**신규 기능 태스크가 전부 닫혔고, 실동작 축도 이제 열렸다.**

「만든 것」과 「확인한 것」 사이의 간격이 이 프로젝트의 가장 큰 위험이었다 —
2026-08-21 시점에는 화면 69개 중 52개를 **아무도 열어 본 적이 없었다**.
**S0-04 가 그 간격을 닫았다**: 화면 **97** 개를 시드 9계정 + 비로그인 **열 축**으로
실제 Chrome 에서 열고, `route.ts` **128** 개를 비로그인·무권한·정당 계정으로 쳤다.
결과는 **`docs/ROUTES.md`** 이며 다시 돌리는 방법도 거기 있다.

### 규모 (실측 · **2026-09-06** · T-00m)

| 항목 | 값 | 2026-09-03(S0-04) 대비 |
|---|---|---|
| 마이그레이션 | `supabase/migrations/` **73개** | +2 (요율 무효화 · 에스크로 배치 어휘) |
| 화면 (`page.tsx`) | **97** | — |
| API 라우트 (`route.ts`) | **131** | +3 (요율 무효화 · 배치 둘) |
| RLS 점검 (`npm run db:rls`) | **1,615 케이스** | +52 |
| 단위 테스트 (`npm run test`) | **2,677** (파일 97) | +35 |
| npm script | **31** (§4) | +7 (점검·검사 계열) |
| 시드 계정 | **9** (§5-1) | — |

> **세는 일을 사람에게 맡기지 않는다.** 화면·API 는 `npm run audit:routes` 가 세고
> 명세 §6·커버리지 표 D 와 함께 대조한다. 나머지는 명령 출력을 그대로 옮겼다.

### 단계별

| 단계 | 상태 | 남은 것 |
|---|---|---|
| 1 기반 | **완료** | — |
| 2 공급 | **완료** | — |
| 3 수요 | `[~]` | S3-01b(소셜 공급자 키 — **외부 계정 발급**) · `wishlist-price-watch` 배치 |
| 4 연결 | `[~]` | S4-08·S4-13 실연동(**외부 계약** D-28) · S4-11 외부 캘린더. **남은 것은 전부 외부 의존이며 개발로 풀 수 없다** |
| 5 거래 | **완료** | — (S5-10 · S5-12~S5-14 까지 닫혔다) |
| 6 플래너 | **완료** | — (S6-03~S6-06 까지 닫혔다) |
| 7 AI·부가 | **완료** | — (**20/20** · T-00k 확정) |
| 8 운영 | **완료** | — (**13/13**). S8-05(CI)의 잔여는 규칙·커버리지·E2E 이며 태스크가 아니라 부채로 남았다 |

### ⛔ 지금 막고 있는 것

1. ~~**FIX-24 로그인 불가**~~ **해소**(S8-01). 시드 9계정 전부 로그인된다 — S0-04 가
   실제 Chrome 으로 열 축 모두 확인했다.
2. ~~**S0-04(실동작 점검) 미착수**~~ **완료**(2026-09-03). 죽은 내비 링크 **0** ·
   도달 불가 화면 **0** · 화면 97 x 계정 10 · API 128 x 계정 10. 결과는 `docs/ROUTES.md`.
   이로써 `docs/TASKS.md` 운영 규칙 5(신규 기능 착수 금지)의 해제 조건이 충족됐다.
3. **실서비스 전 필수 FIX — 남은 것은 둘이다**(`FIX-34` · `FIX-42`).
   ~~FIX-08·11·12·14·22·31·56~~ 일곱을 2026-09-03~06 에 해소했다. 그 과정에서 드러난 것들:
   · `db:rls` 검사 **열다섯이 표가 비어 있어서 통과**하고 있었다(**D-178**)
   · 판정 함수는 있는데 **부르는 자동 경로가 없어** 돈이 묶여 있었다(**D-182** · 배치 둘)
   · 서버 조회가 **1년짜리 캐시**에 얹혀 화면이 **옛 가격**을 말했다(**D-183**)
   · 이름 마스킹이 계약서 문구 30개 중 **26개**를 지우고 있었다(**D-184**)
   **`FIX-34`(next@16)는 D 단계**로 배정됐다 — `FIX-22` 가 캐시 기본값을 닫아 급한 구멍은
   없어졌고 판올림은 화면을 함께 손볼 때가 싸다. **`FIX-42`(AI 회귀 골든셋)가 다음 착수
   대상**이다. 목록·등급은 `docs/TASKS.md` 「알려진 결함(FIX)」.

### 확인의 사각지대 (알고 있을 것)

`npm run lint` · `test` · `build` · `db:rls` · 흐름 스크립트 — **다섯 가지 어디에도
브라우저가 없다.** `db:rls` 는 psql 세션을 갈아 끼우고 흐름 스크립트는 PostgREST 를 직접
두드린다. 그래서 **렌더 오류·깨진 링크·세션 처리 실패는 전부 초록불로 지나간다.**
FIX-24 가 그 사각지대에서 나왔고, **E2E 미도입**(기술 부채 · Playwright 는 S8-05)이 그
사각지대를 만든 원인이다.

**S0-04 가 그 자리에 다리를 놓았다** — `npm run audit:screens`·`audit:api` 가 실제
Chrome 을 CDP 로 몰아 로그인 폼을 지난다(**새 의존성 없이**). 아직 E2E 가 아니다:
**화면을 열고 상태를 적을 뿐 흐름을 끝까지 밟지 않는다.** Playwright 는 여전히 부채다.

### 다음

순서는 `docs/TASKS.md` 「다음 착수 태스크」의 **A-4 → B-1 → C → D → E → F** 가 진실이며
여기서는 **환경 관점**만 적는다.

1. **A-4 · `FIX-42`(AI 회귀 골든셋)** — 다음 착수 대상. 룰 20종 × 샘플 계약서 스냅샷.
   `FIX-31` 이 마스킹 쪽 절반을 이미 만들었다(`lib/core/masking/name-cases.ts` —
   **미탐과 오탐을 같은 표에서 함께 재는** 방식). 그 방식을 룰로 넓힌다.
   **골든셋에는 마스킹본만 커밋한다**(CLAUDE.md §5.6).
2. **B-1 · 실태 조사** — 참가격 표본·업체 인터뷰·수수료 요율. 코드가 아니라 조사이며
   A-4 와 **병행**한다. 값이 없어 서지 않는 자리(O-02·O-15·O-19~21)가 그 결과를 기다린다.
3. **S8-05 잔여(CI)** — ESLint `no-restricted-imports` 규칙, 커버리지 80% 측정
   (`@vitest/coverage-v8` 설치 필요 · **새 의존성**), **E2E(Playwright)**.
   E2E 가 들어오면 `audit:screens` 의 일부가 자동화된다(D-75 의 "갚는 조건").
   **`FIX-59` 도 여기서 갚인다** — 점검 도구가 화면 하나에서 굳는 문제이며,
   E2E 프레임워크가 들어오면 그 타임아웃·격리를 프레임워크가 든다.
4. **`npm run audit:routes` 를 CI 게이트로 올릴지 결정** — 지금은 조사 도구이며
   종료 코드가 항상 0 이다. 죽은 링크·도달 불가가 **0 인 지금이 기준선을 세울 때**다.
5. ~~명세 §6 에 실재 화면 15개 반영~~ → **T-00m 이 다른 방식으로 닫았다.** 표를 늘리는
   대신 **§4·§6 머리에 "표 행 수와 실제 라우트 수의 차이" 를 적었다** — 차이가 결함이
   아니라 알고 있는 차집합이며 전수 대조는 `docs/ROUTES.md` 가 든다는 사실을 명세를 읽는
   사람이 바로 알게 하는 편이, 행 15 개를 옮겨 적고 다음에 또 어긋나는 것보다 낫다.
6. 기록 등급 FIX **17건** 정리(E 단계).

### 개발 순서
07 §1.3의 **8단계**(기반 → 공급 → 수요 → 연결 → 거래 → 플래너 → AI·부가 → 운영)를 따른다.
**단계별 공개는 없고 전체 완성 후 일괄 오픈**한다(D-20). 태스크 번호 체계는 `docs/TASKS.md` 참조.