-- =============================================================================
-- 0040 · 커뮤니티 플래그 — 남은 조건 갱신 (S7-17)
-- 근거: CLAUDE.md §2.1, T-00f, D-24(조율자), docs/07 §2.2 F-V-18
-- =============================================================================
-- 0039 는 "모더레이션 큐(S7-17) 완료 후 개방" 이라고 적었다. **그 조건은 충족됐다** —
-- `/admin/community-reports` 와 처리 API 가 섰고 사유·이력·신호까지 붙었다.
--
-- **그런데 켜지 않는다.** 태그된 업체가 부당 게시물에 **이의를 제기할 경로가 없다**
-- (F-V-18 · S7-16). D-24 는 우리를 조율자로 규정했고 조율자는 **양측에 같은 절차**를
-- 줘야 한다 — 소비자에게는 신고 버튼이 있고 업체에는 없는 상태로 열면, 첫 분쟁에서
-- 업체가 할 수 있는 일은 우리에게 메일을 보내는 것뿐이다.
--
-- 그래서 **행에 적힌 조건을 사실에 맞게 고친다.** 스위치가 왜 꺼져 있는지 DB 가 들고
-- 있어야 다음 사람이 코드를 뒤지지 않는다.
-- =============================================================================
update public.feature_flags
   set rollout_json = jsonb_build_object(
     'reason', '업체 이의 경로(S7-16 · F-V-18) 완료 후 개방',
     'owner', 'S7-16',
     'done', jsonb_build_array('S7-14 스키마', 'S7-15 소비자 화면', 'S7-17 모더레이션 큐')
   )
 where key = 'community.enabled';
